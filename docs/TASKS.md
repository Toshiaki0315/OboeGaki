# TASKS.md — 実装タスクと進捗

hitofude（参照実装）との機能差分 [docs/hitofude-gap.md](hitofude-gap.md) を
優先順に並べ直したバックログ（2026-09-03 起票）。各タスクの根拠
（参照実装のモジュール・ADR・タスク番号）は差分一覧の該当行を参照。

優先順の考え方:

1. **第 1 群** — 毎日の利用で穴になっているもの。小さく作れて効きが大きい
2. **第 2 群** — 書き味と表示の完成度。エディタとしての質を参照実装に揃える
3. **第 3 群** — ノート管理・検索・設定の充実。数が増えてから効いてくる
4. **第 4 群** — 大物・専門機能。単体で設計判断（ADR）が要るもの
5. **待ち** — 外部要因でブロック中

着手の規約: 1 タスク = 1 ブランチ、TDD（RED→GREEN→`make check`→commit）。
着手したらチェックボックスを `[-]`（作業中）にし、完了で `[x]` に変える。

---

## 第 1 群 — 日常利用の穴（最優先）

- [x] **1-1. 前回の vault を記憶して次回起動で自動的に開く**（Q-6）
      保存先はフロント側の設定置き場（localStorage か tauri-plugin-store）。
      開けなかったら黙ってフォルダ選択に落ちる（Q-6 と同じ挙動）
- [x] **1-2. 画像の貼り付け / ドロップ取り込み**（A-2, editor/attachments.py）
      クリップボード画像・ドロップされたファイルを `attachments/` へ保存して
      カーソル位置に `![]()` を挿入。ファイル名は日時 + 一意化
- [x] **1-3. ゴミ箱の後始末**（§7.6, purge_trash / note_actions）
      30 日で自動掃除（vault を開いたときに実行）+「完全に削除」+「ゴミ箱を空にする」。日数は定数で開始し、環境設定（3-9）で可変に
- [x] **1-4. オートペア + URL 貼り付けリンク化**（§5.5-4/5）
      選択して `*` `[` `(` `"` で囲む。URL を選択の上に貼ると
      `[選択](URL)` にする。IME 中のガードと手動チェック追記を忘れない
- [x] **1-5. 文字サイズ変更（Cmd+= / Cmd+-）**（§5.2）
      本文フォントサイズの増減と記憶。永続化は 1-1 と同じ置き場

## 第 2 群 — 書き味と表示の完成度

- [x] **2-1. コードブロックのシンタックスハイライト**（core/code_tokens.py, ADR-0008）
      CM6 は `@codemirror/language-data` の遅延ロードで言語別ハイライトが
      標準機能。アクセント色・ファイル名ラベルも参照実装に合わせる
- [x] **2-2. front matter の隠蔽と解釈**（ADR-0013, core/frontmatter.py）
      `---` ブロックの装飾（隠蔽 or 淡色）と、id / pinned / tags の読み取り。
      2-3 ピン留めの前提
- [x] **2-3. ピン留め**（§7.3。2-2 に依存）
      front matter `pinned: true` に永続化、一覧の先頭固定、ピン中は削除ガード
- [x] **2-4. 見出しの折りたたみ**（ADR-0019, core/folding.py）
      CM6 の foldService に見出し区間を返すだけで乗る見込み
- [x] **2-5. 脚注 `[^1]` の装飾と裸 URL のリンク化**（B-3, inline_scanner）
      裸 URL は Lezer 拡張のカスタムノードとして追加し、activation に乗せる
- [x] **2-6. 表の仕上げ**（ADR-0003 決定 4 / ADR-0028, core/table.py）
      セル内 `<br>` の描画、行 × 列を指定する挿入コマンド、
      エディタを離れたときのソース整形（桁揃えは ADR-0044 でやめた）
- [x] **2-7. 複数行選択の行単位トグルと見出しサイクル**（B-1）
      箇条書き / 番号 / 引用を選択行全部に揃えて適用。cycle_heading 相当
- [x] **2-8. プレーンテキストとしてコピー（Cmd+Shift+C）**（§5.4）

## 第 3 群 — ノート管理・検索・設定

- [x] **3-1. タグの厳密絞り込みとタグ補完**（C-4 / H-3, index の tags 結合）
      サイドバーのタグクリックを全文検索代用から tags テーブル結合に替える。
      `#` 入力中の補完（↑↓ / Enter / Tab）も一緒に
- [x] **3-2. テンプレートと同梱マニュアル**（E-4, resources/templates / MANUAL）
      `templates/` の雛形から新規作成・日次テンプレート・初回シード。
      「覚書の使い方」ノートのシードも同じ仕組みに乗せる
- [x] **3-3. フォルダの作成・改名とフォルダツリー**（ADR-0024, ui/sidebar.py）
- [x] **3-4. バックリンクとノートリンク補完**（E-6, core/notelink.py）
      索引に links テーブルを導入（スキーマ世代 +1）。本文下の帯と
      `[[` 補完をセットで
- [x] **3-5. 検索式**（core/searchquery.py）
      語の AND / OR・タグ条件。参照実装の構文をそのまま移植
- [x] **3-6. クラッシュ退避**（H-1, autosave.stash / recovery）
      未保存の内容を退避し、起動時に復元を提案。退避先は管理フォルダでは
      なく**アプリのデータフォルダ**（vault ごとに分ける）— 保存できない
      理由が vault 側にあることが多く、書けない場所の保険は保険にならない
      （参照実装 autosave.recovery_root と同じ判断）
- [x] **3-7. 二重起動の安全化**（H-1, acquire_vault_lock）
      vault ロック（Tauri は single-instance プラグインが使える）
- [x] **3-8. ウィンドウ位置・サイズの保存**（config.window_geometry）
- [x] **3-9. 環境設定ダイアログ**（ui/preferences.py, N-1）
      フォントサイズ・履歴の間隔・ゴミ箱の日数・本文幅・テーマ手動切替。
      1-3 / 1-5 / 本文幅の受け皿。設定の置き場は 1-1 で決めたものに揃える
- [x] **3-10. ステータスバーの統計**（core/stats.py）文字数・行数・保存時刻
- [x] **3-11. ペイン開閉（Cmd+1 / Cmd+2）と幅のドラッグ調整・保存**（§5.4）
      spec の 3 ペインに対し、覚書は左を 1 列にまとめてある。`Cmd+1` は
      サイドバー相当（フォルダ・タグ・ゴミ箱）、`Cmd+2` はノート一覧に割り当て、
      両方閉じたら左のペインごと畳む
- [x] **3-12. 見出しパレット（Cmd+R）**（C-2）
- [x] **3-13. ファイルと索引を手で合わせ直すメニュー**（M-6）
- [x] **3-14. 外部で削除されたノートの「再作成しますか？」**（§7.5）
- [x] **3-15. ノートの複製ほか一覧の右クリックメニュー**（ui/note_actions.py）
      ピン留め・複製・フォルダへ移動・テンプレートに登録・リンクのコピー・
      Finder で表示・ゴミ箱へ移動。改名は題名の欄が担うのでメニューには出さない
- [x] **3-16. 未使用の添付の片づけ**（E-5。1-2 の後で）
- [x] **3-17. 保存した検索**（ui/sidebar.py。3-5 の後が自然）

- [x] **3-18. 新規ノートは絞っているフォルダの中に作る**（要望 2026-09-07）
      ＋ 新規 / Cmd+N の置き場所を `folderFilter` から決める（直下・ゴミ箱・
      絞っていないときは直下）。判断は `lib/folder-tree.newNoteFolder`
- [x] **3-19. ノート一覧の行を 4 段にする**（要望 2026-09-07）
      題名（ファイル名の幹）／本文の冒頭／フォルダのパス／更新日付。
      直下のノートにはフォルダの段を出さない。「詰める」ではフォルダの段も畳む

- [x] **3-20. 一覧の上を 3 段にまとめる**（要望 2026-09-07）
      検索欄 → 絞り込みのラベル → [並び順][＋ 新規]。並び順と「＋ 新規」は
      `components/ListControls` で横に並べ、一覧より上に固定する（一緒に
      スクロールしない）。ゴミ箱と検索中は並び順を出さず「＋ 新規」だけ残す
      「＋ 新規」の右クリックで「テンプレートから新規…」「今日のノート」を選べる

- [x] **3-21. 画像の説明をキャプションと Tip に出す**（要望 2026-09-08）
      `![説明](…)` の説明を絵の下に小さく出し、載せたときの title にも入れる。
      説明が無ければ従来どおり絵だけ（文中の小さな絵の行を割らない）

- [x] **3-22. 起動時に前回開いていたノートを開く**（要望 2026-09-08。参照実装
      session/last_note）。保管フォルダごとに相対パスで覚え（`lib/last-vault`）、
      まだあればそれを、無ければ一番上のノートを開く。閉じたら忘れる

- [x] **3-23. サブフォルダを畳める**（要望 2026-09-08）
      子を持つフォルダにだけ三角（▼/▶）。押しても絞らず開閉だけ。既定は
      全部開く（参照実装の expandAll と同じ）。畳んだ状態は localStorage に覚える

- [x] **3-24. 直下が 0 のフォルダは中の合計を括弧で出す**（要望 2026-09-08、
      ADR-0024 追記 6）。中にフォルダしか無い親が 0 に見えて「空」と誤解される。
      数字は直下のまま、直下 0 かつ中にノートがあるときだけ `(3)` と薄く出す

- [x] **3-25. SVG も画像として貼れる・描ける**（要望 2026-09-09、ADR-0004 追記）
      Finder から `.svg` を落として `attachments/` へ。本文では幅の無い SVG
      （draw.io・Excalidraw の書き出し）を viewBox の大きさで置く（300×150 に
      潰さない）。HTML 書き出しも同じ幅を書く。PowerPoint は Mermaid と同じく
      PNG に描き直して置く（開く側が SVG を描けないことがある）

- [x] **3-26. 外部変更は中身を見比べてから聞く**（実機 2026-09-09）
      SVG を貼った直後に打っていると競合の 3 択が出た。監視のイベントは抑制窓
      （保存後 1.5 秒）を過ぎて届くことがある（同期ソフトが上げ終わったあとに
      ファイルを触り直す。参照実装も iCloud で同じ現象を見ていた）。読んだ中身が
      「開いた・書いた・採用した本文」か画面の本文と同じなら外部の変更ではない
      として何もしない。本当に中身が違うときだけ読み直す・聞く（spec §7.5 の
      「hash で再検査」に相当）

- [x] **3-27. パスの相対部分を NFC に揃える**（実機 2026-09-09、ADR-0050）
      テキストエディタ（Cocoa）で書き換えると、監視イベントが NFD のパスで届き、
      索引に同じノートが 2 行できて一覧に 2 つ並んだ。走査・索引のキー・監視
      イベント・無視リストで NFC に揃える。残っていた NFD の行は次の起動の
      sync が消す。フロントも開く入口（openNote）で揃える（前回のノートの
      記憶に NFD が残っていた）

- [x] **3-28. 表の空セルを空のまま描く**（実機 2026-09-10）
      `|  | aaa |` の左上や途中の空セルが消え、後ろのセルが左へずれていた。
      Lezer の Table は中身の無いセルに TableCell ノードを作らないため。
      セルは区切り `|` の位置で数え、ノードが乗っていない範囲は空セルにする

- [x] **3-29. 箇条書きの折り返しを点・番号の後ろに揃える**（要望 2026-09-10）
      行に `--hang`（先頭の空白の字数 ch + 印の幅）を付け、1 行目だけ
      text-indent で戻す。番号は隠さず、同じ幅の箱（inline-block）にして
      フォントの字幅に依らず揃える。カーソルが乗っている間も外さない

- [x] **3-30. 見出しとファイル名を両方向で揃える**（要望 2026-09-10、ADR-0005 追記）
      題名欄 → 見出しは Rust（with_title）に既にあった。見出し → ファイル名を
      足す: 保存後に H1 が変わっていて、ファイル名が前の見出しに従っていた
      ときだけ改名（参照実装 _rename_if_title_changed）。見出しの装飾（HTML タグ・
      強調・打ち消し・リンク）は題名に持ち込まない（stripInline、2026-09-11）。
      エディタは作り直さず、
      予約の書き先だけ付け替える（useNoteSync.renamed）

- [x] **3-31. タイトルバーに文書の題名**（要望 2026-09-10）
      「題名 — おぼえがき」（macOS の慣例）。開いていなければアプリ名だけ。
      改名にも追従する（currentPath に効く）。権限 `core:window:allow-set-title`

- [x] **3-32. 本文の幅を 5 段に、既定を 56rem に**（要望 2026-09-10、ADR-0018 追記）
      狭め 46 / 標準 56（既定）/ 広め 68 / より広め 82 rem / 最大

- [x] **3-33. アイコンの紙に縁の線を引く**（実機 2026-09-10）
      「について」の白い窓で紙の白が地に溶け、絵の境が消えていた。元絵は
      scripts/make_icon.swift（参照実装 make_icon.py の移植。依存なし）、
      `make icon` で全サイズ再生成。縁の明度をテストで見張る

- [x] **3-34. 版を 0.5.0 に、「について」にビルド日時**（要望 2026-09-10）
      Cargo / tauri.conf / package.json の 3 箇所を揃える（テストで見張る）。
      日時は `make app` / `make dmg` が環境変数で build.rs に渡し、
      「Version 0.5.0 (2026-09-10 13:05)」の括弧に出る。開発版は「開発版」

- [x] **3-35. フォルダを Drag & Drop で別のフォルダへ移す**（要望 2026-09-10、
      ADR-0024 追記 7）。行を掴んで別のフォルダ／見出し（直下）に落とす。
      自分の中・今の親には落とせない。Rust `Vault::move_folder` + `folder_move`

- [x] **3-36. 起動時にノートが無ければ「無題」を作る**（要望 2026-09-10）
      判断は lib/startup-note。一覧が空なら**ディスクを見て**（vault_is_empty）
      決める — 索引の無い vault で中身があるのに作らない。合図（index-updated）
      待ちはリスナー登録より先に飛ぶと永遠に待った（実機）。初めて開く空の
      vault では種撒き（seed_manual）が先に走るので「おぼえがきの使い方」が開く

- [x] **3-37. 保管フォルダを選び直したら索引を作り直す**（要望 2026-09-10）
      chooseVault（環境設定の「変更…」・メニュー・最初の画面）の後で
      `index_sync(full)`。起動時の開き直しは従来どおり差分同期

- [x] **3-38. ノート一覧の複数選択と、まとめて Drag & Drop**（要望 2026-09-10、
      ADR-0024 追記 8）。Cmd+クリックで足し外し、Shift+クリックで範囲。選んだ
      行を掴むと全部を運び（目印は改行区切り）、フォルダへ落とせば全部移す、
      ゴミ箱なら確認 1 回でまとめて捨てる。判断は lib/note-selection

## 第 4 群 — 大物・専門機能（各々 ADR を書いてから）

- [x] **4-1. 数式**（ADR-0036 で 0009 / 0020 を置換）
      Temml で LaTeX → MathML。画面（CM6 widget）も HTML 書き出しも
      同じ文字列を使い、組むのは WebKit。同梱は実測 +215KB
- [x] **4-2. Mermaid 図**（ADR-0037 で 0021 を置換）
      同じ WebView で描き、書き出しには SVG を埋める。動的 import なので
      主バンドルは +2KB（合計 +3.4MB は開くまで読み込まれない）
- [x] **4-3. PDF 書き出し・印刷**（ADR-0038 で 0007 の PDF 経路を置換）
      `Cmd+P` で macOS の印刷パネル（「PDF として保存」もそこから）。
      書き出しと同じ本文を隠しの領域に組んでから刷る
- [x] **4-4. HTML 書き出しのコード色分け**（core/html。2-1 と同じ字句解析を再利用）
      画面と同じ Lezer のパーサで色分けし、実色は書き出した HTML の
      CSS 変数が持つ（1 枚で完結したまま読む人の明暗に合う）。
      印刷（4-3）にも同じ色が乗る（紙はライトの組で刷る）
- [x] **4-5. PowerPoint 取り込み / 書き出し / スライド分割**（ADR-0039）
      割り方は純関数（lib/slides.ts）、書き出しは pptxgenjs、取り込みは
      jszip + DOMParser。どちらも動的 import
- [x] **4-6. 外部形式の取り込み**（ADR-0040。PDF は pdf.js、整え方は
      参照実装 core/imported.py のまま）。**文字だけ**を読む — 埋め込み
      画像と、文字の無い PDF の読み取り（OCR）は 4-7
- [x] **4-7. OCR**（ADR-0041 で 0027 の決定 2 を置換）
      Swift の同梱は要らなかった — Rust から Vision を直に呼べる
      （実測 0.73 秒。同梱物も署名の対象も増えない）。**ローカルLLM の
      読み手**（決定 1 の 2 択）は 2026-09-07 に移植 — 環境設定の
      「文字の読み取り」が 1 択のまま置かれていた（ユーザー指摘）
- [x] **4-8. ローカル LLM アシスタント**（ADR-0025 のまま。Ollama への口は
      標準ライブラリの TcpStream で書き、依存は増えていない）。要約・
      レビュー・質問の 3 つ。**出典付きの横断検索（L-2 以降）はまだ**。
      本物の Ollama で実測（gemma3:4b で要約 8.6 秒 / 降ろす道も効く）
- [x] **4-9. リンクの図（グラフ表示）・続柄・仮身化**（M-1〜M-3）
      図は Mermaid（ADR-0037 で積んだもの）に描かせる — 力学モデルを
      自前で持たない。続柄は箇条書きの `:` より前（新しい記法を作らない）
- [x] **4-10. 関連ノートの提示**（core/related.py）
      **モデルには選ばせない** — 根拠は索引の中にあるので、Ollama が
      無くても理由付きで出る
- [x] **4-11. 文章チェック**（core/style_check.py）
      **指摘するだけで直さない。** 形態素解析は使わず言い回しの辞書に絞る
      （1 つの誤検出が以後の指摘を全部無視させる）。まずパレットで出す
- [x] **4-12. front matter の ULID id を索引・履歴の鍵にする**（ADR-0023）
      → **入れない**と決めた（ADR-0042）。付け替えの抜け（ゴミ箱→復元）は
      塞ぎ、外での改名で履歴が途切れることは受け入れて使い方ノートに書いた。
      覆すときの引き金も ADR に残した

## 第 5 群 — PowerPoint の見栄え（md2pptx 相当へ）

いまの書き出しは「ざっくり作って手で整える」構え（ADR-0039）。それは
残したまま、**手で整える量を減らす**。上から効く順に並べてある。

質問（2026-09-05）の調べで分かったこと: pptxgenjs は**ゼロから組む**道具で、
既存の .pptx をテンプレートとして読む口が無い（型定義に該当 API 無し）。
5-6 だけ道具立てが変わる。

- [x] **5-1. インライン装飾を落とさない**（太字・斜体・打ち消し・インライン
      コード・リンク）。いまは `slides.ts` の `plain()` が記号ごと落として
      いて、素の文字になる。**差がいちばん大きいのはここ**。
      pptxgenjs の `addText` はリッチテキストの配列を受ける
- [x] **5-2. 表とコードの見栄え**（見出し行の塗り・罫線・列幅、等幅と背景）
- [x] **5-3. スライドの体裁**（スライド番号・フッタ・セクション扉）。
      いまは 2 つ目以降の `#` が**捨てられている**ので、扉にして拾う。
      共通の体裁は `defineSlideMaster`
- [x] **5-4. 段組み / カード**（横並びの箱）。**新しい記法は作らなかった** —
      スライドの中の `###` が 2 つ以上あればそれぞれ箱になる。表・コードが
      あるときや、小見出しの前に本文があるときは今までどおり縦に流す
- [x] **5-5. 見た目の設定**（`slide-font` / `slide-mono` / `slide-accent` を
      front matter から。**ノートと一緒に運ぶ** — 設定に置くと資料ごとに
      変えられず、別の機械で開いたときに再現しない）
- [x] **5-6. テンプレート .pptx の適用**（ADR-0045 案 A）。環境設定で
      テンプレートを選ぶと、その**配色と書体**でスライドが出る。図形の色は
      テーマ参照にしたので、PowerPoint 側でテーマを替えても追従する。
      背景の飾りとロゴまで要るなら案 B（別の ADR）

- [x] **5-x. 枠は中身の分だけ置く**（実機報告 2026-09-08）
      本文の枠を固定の 78% で置き、その下にコード・表を各 28% で積んでいた
      ので、本文 1 行 + コード 2 つで紙からはみ出し、表は残り 1in に押し込ま
      れていた。`bodyFrames` が `estimateHeightIn`（割る側・見張りと同じ物差し。
      `slide-frame.ts` に移動）で測って積む。表の行はセルの余白も見積もる

- [x] **5-y. Mermaid を図として書き出す**（要望 2026-09-08）
      `mermaid` のコードブロックを SVG → PNG（WebView の canvas、2 倍）にして
      画像と同じ置き方（横の用紙では本文の右）に。描けなかった図はコードのまま
      残す。プレビューと見張りも図を画像として数える

## 第 6 群 — 書くときの近道（Qiita の `/` メニュー相当）

要望（2026-09-05）。Qiita のエディタは空行で `/` を打つと候補が出て、
選ぶと記法が入る。**並べるものの多くは覚書に既にある** — `:::note`・表・
脚注・コードブロックは実装済みで、足りないのは折りたたみだけ。

- [x] **6-1. `/` の候補メニュー**。補完の仕組みは `#`（タグ）と
      `[[`（ノートリンク）で既に動いているので、**行頭の `/` を見る
      候補源を足す**。挿入はスニペットでカーソルの置き場所まで決める
      （コードなら ``` の中、表なら 1 つめのセル）。- **空行の行頭だけで出す** — 日付 `2026/09/05`・URL・`and/or` で
      誤爆させない - **変換中は出さない**（T5。タグ補完と同じガード）- 並べるのは既にある機能への近道だけ。**AI に続きを書かせる候補は
      入れない** — アシスタントは「本文は書き換えません」と画面で
      約束している。入れるなら別扱いで、別のタスクにする
- [x] **6-2. 折りたたみ**（`:::details たたんだときの見出し` … `:::`）。
      **書くときは `:::note` の仲間**（案 A）。生の HTML は書き出しで
      無効にしてある（`html: false`）ので、`<details>` を通す道は開けない。
      ただし **Qiita から貼った `<details><summary>…` は読むときだけ受ける**
      （案 B の片道）。畳めないと、貼った本文が読めないまま残るため - エディタ: `note-container` を手本に行の並びで見つけて、
      開閉できる装飾にする（**木のノードは作らない**）- 書き出し・印刷: markdown-it の container で `<details><summary>` - できたら 6-1 のメニューに `details` を足す

- [x] **6-3. コードの言語の補完**。` ``` ` の直後で言語名の候補を出す
      （要望 2026-09-05。Qiita と同じ入口）。- **候補は「実際に色が付く言語」だけ**。色付けと同じ
      `@codemirror/language-data`（143 言語 / 名前と別名で 182 個）から
      引くので、選んだのに色が付かない、が起きない - 画面には `cpp` と `C++` の両方を出す（打つ綴りと読める名前）- **1 文字打ってから出す**。` ``` ` だけで一覧を出すと、ただの
      コードブロックを書きたいときに Enter が候補の決定に化ける - `python:aaa.py`（ADR-0008）は `:` より前だけを補完する
- [x] **6-4. コードブロックを濃い地にする**（要望 2026-09-05）。
      ライトのとき、Pygments friendly 系の色が薄い地に乗って読みにくい。
      **Qiita と同じで、明暗どちらでもコードだけは濃い地**にする。- 画面（`cm-codeblock-line`）と HTML 書き出しの両方 - 地が濃くなるので、**字の色は github-dark 系に一本化**し、
      文字色とキャレットの色も行に持たせる（ライトのままだと
      黒いキャレットが濃い地に沈む）- **紙は今までどおり白地・ライトの組**（`@media print`）。
      濃い地はインクを食うし、既にそう決めてある - インラインの `コード` は変えない（本文の中で浮く）

- [x] **6-5. ゴミ箱をフォルダの木の中へ**（要望 2026-09-05）。左下は
      **フォルダとタグの 2 つだけ**にして、ゴミ箱はフォルダの中の 1 行に
      した。押すと**一覧が捨てたノートに変わる**（参照実装の
      `FilterKind.TRASH` と同じ組み立て。あちらでも一覧の絞り込みの
      1 つになっている）。- 一覧の右クリックは**「元に戻す」「完全に削除…」だけ**。ゴミ箱の
      中身にピン留めや改名を許すと、戻したときの状態が読めない
      （参照実装 note_actions と同じ判断）- **ゴミ箱は索引に無い**（T7 の走査対象外）ので、一覧の中身は
      `trash_list` から。並びは捨てた新しい順に固定（更新順 / 名前順の
      選びは出さない）- 落とし先としての振る舞い（ノートを落とすと捨てる・右クリックで
      空にする・Finder で開く）はそのまま

- [x] **6-6. 手引きに載っていない操作を書く**（調べ 2026-09-06。
      [other-editors-gap.md](other-editors-gap.md) §4）。**実装ゼロで
      機能が増える**。行の移動・行の複製・ノートの中の置換。
      複数カーソルは**動かないので書かない**（同 §4 の注）
- [x] **6-7. メニューから PDF で書き出す**。今も `Cmd+P` →「PDF として
      保存」でできるが、気づかれない。中身は印刷と同じ道でよい
- [x] **6-8. 画像の大きさ指定**。写真を貼ると本文幅いっぱいに出て縮め
      られない。記法は `![説明|300](道)`（Obsidian と同じ）。
      **ほかのビューアで開いても画像が壊れない**ほうを採る —
      `![](img =300x)` 系は URL が壊れる
      - [x] **6-8b. 掴んで変える**（要望 2026-09-15）。絵の右下のつまみを
            引くと絵だけが先に変わり、離したときに行の `|幅` を書き換える
            （真実は本文 = T1。書き出しにもそのまま効く）。幅だけ書き、縦は
            形なりに縮ませる。つまみの上のマウスはエディタに渡さない
            （行が開いて絵が消える）。`withImageWidth` / `clampImageWidth`
            とウィジェットの jsdom テスト

## 第 7 群 — ポメラから取り入れる（調べ: [pomera-ideas.md](pomera-ideas.md)）

要望 2026-09-06。**足すのは日本語で長文を書く道具立てだけ**に絞る
（あの機械の価値は引き算なので、機能を全部持ってくるのは筋が悪い）。

- [x] **7-1. アウトラインで節ごと動かす**（ポメラの目玉）。今は飛ぶだけ。
      見出しを選んで**その節を丸ごと**上げ下げできるようにする。- 範囲は `headingSection`（6-2 で直したもの）をそのまま使う - **本文を書き換える操作**なので、`Cmd+Z` 1 回で戻ること - 出し方はアウトラインの右クリック（この app の作法）
- [x] **7-2. 選んだ語を辞書で引く**。ポメラは電子辞書を積んでいる。
      Mac には辞書があるので `dict://` に渡すだけ。**外へ出ないので
      確認の窓は出さない**（生成 AI に渡すのとはそこが違う）
- [x] **7-3. 原稿用紙の枚数**。日本語の書き手は 400 字詰めで測る。
      **単語数は出さない**という既存の判断（語の区切りが無い）は変えない
- [x] **7-4. 行番号の表示**。人と原稿をやり取りするとき「◯行目」で話せる。
      設定で on/off（既定は off — ふだんは要らない）
- [x] **7-5. 日付を選んでその日のノートを開く**。「今日のノート」はあるが
      **昨日・先週に戻れない**。日記に使う人に効く
- [x] **7-6. Shift_JIS / CRLF のテキストを読む**。ポメラや Windows で
      書いた `.txt` がそのまま開ける。**読めなかったときだけ**別の文字
      コードで読み直す（当てずっぽうは危ない）。書き出しは UTF-8 / LF のまま

## 第 8 群 — 環境設定「PowerPoint」タブ（[OboegakiPptxSettingsSpec.md](OboegakiPptxSettingsSpec.md)）

仕様 v1.0 を読んで起こした（2026-09-06）。**項目 ID（CFG-xx / UI-xx /
GR-xx / ST-xx / VA-xx / PV-xx）を実装とテストの名前に必ず書く**（仕様 0 章）。

**先に押さえておくこと。** この仕様は「出力仕様（`oboegaki-pptx-export-spec.md`）
の L1 層」として書かれているが、**その出力仕様がこのリポジトリに無い**。
仕様の中で参照している `G-xx`（グリッド）・`T-xx`（タイプランプ）・
`SH-xx`（情報密度）・`L-01/L-04`（溢れ・コントラスト）・`MEAS-01`（幅
テーブル）・`C-xx`（レイアウト）・Frame IR・lint は、**今の覚書には無い
仕組み**。今の書き出しは「ざっくり作って手で整える」構え（ADR-0039）で、
`slides.ts`（割り方）と `pptx.ts`（描き方）だけでできている。

そこで **8-1〜8-4 は今の作りに載る範囲**、**8-5〜8-7 は下地が要るもの**に
分けた。下地の要る側は、出力仕様を持ち込むか、覚書の作りに合わせて
仕様を読み替えるかを決めてから着手する（→ 8-0）。

- [x] **8-0. 出力仕様の扱いを決める**（→ [ADR-0046](adr/0046-pptx-settings-tab.md)）。
      決めたこと: ①設定仕様だけを読み替える（出力仕様は持ち込まない。
      8-5〜8-7 は**幅テーブルが入るまで着手しない**）②テンプレートは
      **配色プリセットの取り込み**にする（選んだ時点で設定の値になる）
      ③色は「テーマ参照」と「6 桁の具体色」の 2 つを持てる型にする
      ④文書ごとの上書きは **front matter のまま**（サイドカーを作らない。
      アプリからは書き込まない）
- [x] **8-1. 設定の器（Phase A）**。`PptxSettings` の型・既定値
      （付録 A）・`SettingsStore`（ST-01）・検証（ST-03）・移行
      （ST-04 / CFG-90）。**設定全体を初期化しない** — 欠損キーは既定で
      補い、未知のキーは残し、壊れた値だけ戻す。
      受け入れ: 欠損・未知・型不正・古い version の 4 ケースで復旧できる
      （純関数なのでテストしやすい。ここは今すぐ書ける）
- [x] **8-2. タブと配線（Phase B、今の作りに載る範囲）**。環境設定に
      「PowerPoint」タブを足し、**今の書き出しが実際に使う値**を配線した。- **入れたもの**: 分ける見出し（CFG-40）・見出しの色（CFG-12。
      テーマ参照 / 具体色）・書体（CFG-30/32）・テンプレート（一般タブ
      から移設）・フッタ 3 つ（CFG-50〜52）・コードの言語名（CFG-73）- **入れなかったもの**（つまみが効かないものは出さない）: 余白
      （CFG-44 → 8-3）・情報密度と箇条書きの上限（CFG-41/42 → 8-5）・
      書き出し前チェック（CFG-70 → **8-10 で入れた**）・続きの接尾辞
      （CFG-43。長い枚を割る仕組みが無いので出番が無い）・画像の説明
      （CFG-72 → **あとから入れた**。alt を `slides.ts` が運ぶようにした）- 即時反映・即時保存（UI-03）。`[既定に戻す]` はこのタブぶんを 1 つ
      （UI-04 の「節ごと」は、節が増えたら足す）- 受け入れは Frame IR の代わりに**出来上がった `.pptx` の XML**で
      見る（UI ではなく結果で確かめる。既存の pptx.test.ts と同じ道）
- [x] **8-3. 用紙サイズ（Phase C の前半）**。7 プリセット + カスタム。
      `deriveGrid()` / `slideMetrics()` を唯一の入口にして、`pptx.ts` は
      そこから貰うだけにした（GR-01）。用紙は `defineLayout` +
      `pres.layout` で切り替える（GR-06）。字の大きさは用紙の高さ
      （GR-02）と設定の大小（CFG-38）を掛けてから丸める。- 受け入れ: 出来上がった `.pptx` の `sldSz` が選んだ用紙になること
      （EMU をインチから出して照合）。縦の用紙で高さ > 幅 - **grep の結果**: 用紙の大きさで変わる値（余白・本文の始まり・
      フッタ・表紙の題・本文とコードの高さ）は全部 `slideMetrics()` 由来に
      した。残っている数値は**字の枠の高さ**（0.3in など）で、用紙が
      変わっても変える必要が無いもの - 画面にも出した: 用紙・余白・字の大きさ（CFG-01/02/44/34）
- [x] **8-4. 縦向きの退避（Phase C の後半）**。縦（H > W）ではカードを
      1 列 × N 段に落とす（GR-04）。横に 3 つ並べると 1 つが細長い帯に
      なって、字が縦 1 列で落ちてくる。
      受け入れ: 出来上がった `.pptx` の箱の位置で見る（横なら y が同じ・
      縦なら x が同じで y が増える）。**画像＋文章も縦積みにした**
      （あとから追加）。割り方は `bodyLayout()` の 1 か所に集めたので、
      書き出し・プレビュー・溢れの見張り・枚割りが同じ幅を見る
- [x] **8-5. 情報密度（CFG-41 / 42 / 43 / 46）**。収まらないぶんは
      **次の枚へ送る**。字を縮めたり、書いた文を削ったりはしない
      （ADR-0039 の構えを崩さない）。- 「要点のみ」は段落を発表者ノートへ回す（CFG-45 の言い方どおりの
      動き）。「詳しく」でも溢れは許さず、割って対応する（CFG-46）- 箇条書きの上限（CFG-42。3〜10）と続きの印（CFG-43）を画面に出した - **1 つで溢れる塊はそのまま置く**（無限に割らない）。そこは見張り
      （CFG-70）が知らせる - **物差しは見張りと同じ**（`estimateHeightIn`）。別々に測ると
      「割ったのに溢れている」が起きる - 受け入れ: 割ったあとに `overflowingSlides()` が空になること
- [x] **8-6. プレビュー（Phase D）**。設定タブの上に、見本 4 種
      （表紙 / 箇条書き / 比較 / 画像と文章 = PV-02）と「このノート」
      （PV-03）を出す。200ms 置いてから組み直す（PV-04）。- **置き場所は書き出しと同じ計算**（PV-01 / VA-04）。本文の枠を
      `slide-frame.ts` に切り出して、`pptx.ts` もプレビューもそこから
      貰う。用紙は `slideMetrics()`、枚割りは `splitForDensity()` と、
      どれも 1 本しかない道を通す - **ただし「一致する」とは言わない**（PV-05 より強い但し書き）。
      字の幅は近似（8-10）なので、**形と収まり具合の目安**まで。
      画面にもそう書いた - 字は線で表す（読ませるためではなく、量を見せるため）- 縦の用紙での画像＋文章の縦積み（GR-04 の残り）も入れた。
      プレビューにもそのまま出る
- [x] **8-7. 警告（Phase E）**（できる範囲で完了。残りは見送り）。- **入れたもの**: 色の読みやすさ（CFG-19 / L-04。具体色を選んだ
      ときだけ、白い紙に置いた見えかたを比で出す）と、用紙を変えた
      ときの再チェック（GR-05。設定を触った瞬間に「◯ 枚で収まらない」）- **見送り**: 同梱していない書体の警告（CFG-35 / MEAS-10）。
      **うちは全部の書体が近似**なので、どの書体を選んでも同じ警告が
      出ることになる。いつも出る警告は読まれない →
      [IDEAS.md](IDEAS.md) に理由と引き金を書いた - **測れない場合がある**: 色が「テーマに従う」のときは実色が
      分からない（PowerPoint 側で決まる）ので、比を出さない
- [x] **8-8. 発表者ノート（CFG-60 / 62）**。8-5 で「要点のみ」が本文を
      スライドから外すようになったので、ここが意味を持った。- **既定は残す**（CFG-60）。切るときだけ確認を挟む（CFG-62）。
      文言は覚書向けに直した: **「ノートの本文そのものは消えません」**を
      添える（仕様の「失われます」だけだと `.md` が消えると読める）- **CFG-61（AI で要約）は見送り**（CFG-63 も一緒に）。手元の LLM を
      書き出しのたびに枚数ぶん回すことになり、効き目も読めない →
      [IDEAS.md](IDEAS.md) に理由と引き金を書いた

- [x] **8-9. CLAUDE.md への追記**（仕様 11 章を ADR-0046 に合わせて）。
      設定に置くものの線引き・**効かないつまみを出さない**・グリッドは
      `slideMetrics()` から・色はテーマ参照と 6 桁の 2 本立て・文書ごとの
      上書きは front matter（サイドカーは作らない）・優先順位は
      front matter > 環境設定・結果の XML で試験する、を規約に足した

- [x] **8-10. 文字の幅を測る（MEAS-01 の代わり）**。8-5〜8-8 の土台。
      **本物の幅テーブルは積めない** — 測る相手は「出力先の PowerPoint が
      使う書体」で、こちらの機械にあるかどうかは関係が無い（CFG-36）。
      同梱するなら書体そのものの計量値が要り、権利の話にもなる。
      そこで **字の種類ごとの近似**（和文は 1 文字ぶん、欧文は細い / 並 /
      太いの 3 段）で測り、**安全のぶんを上乗せする**（仕様 MEAS-10 の
      「一覧外の書体」の扱いを全部に広げる）。近似であることは画面にも書く。- `measureIn()`（幅）と `wrapCount()`（何行になるか）を純関数で - 和文は**どこでも折れる**、欧文は空白で折れる、を分ける - 受け入れ: 実際に溢れる例／収まる例で行数が合うこと

## 第 9 群 — 保守（ソースコードレビュー 2026-09-07 の残り）

対応済み: HTML 書き出しの属性値エスケープ（②）・Rust テスト名の警告（④）・
App.tsx の分割（①、ADR-0048。23 段で 6,020 行 → 3,443 行）。

- [x] **9-1. Tauri の CSP を設定する**（③）— 2026-09-07 本番 .app で数式・図・画像・PDF/PPTX 取り込み・書き出し・アシスタント・印刷を実機確認
      `tauri.conf.json` の `security.csp` が `null`。WebView から fetch /
      convertFileSrc / 外部フォントは使っていないので
      `default-src 'self'; script-src 'self'; style-src 'self' 'unsafe-inline';
img-src 'self' data: blob:` 程度から始める（`'unsafe-inline'` は CodeMirror
      の style-mod と Mermaid の SVG に必須）。**`make run` で Temml・Mermaid・
      画像・Ollama 連携が壊れないことを見ながら**入れる。dev は `devCsp` も
- [x] **9-2. ESLint（react-hooks）を `make check` に入れる**（⑤）— 2026-09-07。React Compiler 向けの 4 規則は理由を書いて切った（eslint.config.js）
      `eslint-disable-next-line react-hooks/exhaustive-deps` が App.tsx に 3 箇所
      あるのにリンタが無い。`eslint` + `eslint-plugin-react-hooks`（または
      oxlint / biome）を devDeps に足し、Makefile の check と CI に加える。
      最初は既存の指摘を洗い出し、直すものと許すものを分けてから緑にする
- [x] **9-3. App.tsx の状態と処理を hooks に切り出す**（ADR-0049）— 2026-09-07。useAssistant / useSearch / useNoteSync の 3 本（App.tsx 3,443 → 2,869 行）。保存・外部変更・競合・外部削除は実機で確認
      部品化で残った 3,443 行の大半は状態と処理。`useAutosave`（自動保存・
      退避・競合）、`useSearch`（検索・絞り込み・保存した検索）、`useAssistant`
      （Ollama・答え・関連）のような hooks に分け、テストは
      `@testing-library/react` の `renderHook` で。別の ADR を書いてから
- [x] **9-4. `stores/app.ts` の IPC ラッパーを分ける**（⑥）— 2026-09-07。`src/lib/ipc.ts` へ（ストアは 437 行 → 52 行）
      Zustand のストア定義と 40 件超の `invoke` ラッパーが同居。ラッパーを
      `src/lib/ipc.ts` に移し、ストアはストアだけにする。9-3 と一緒にやると
      import の張り替えが 1 回で済む
- [ ] **9-5.（低）`llm.rs` のテストがループバック通信に依存**（⑦）
      CI は macos-14 で問題なし。ネットワーク無効の環境で回す予定ができたら、
      `Read + Write` のトレイト境界で通信部を差し替えられるようにする

## 第 10 群 — MCP サーバ（[ADR-0051](adr/0051-mcp-server.md)。読み書き両方）

Claude Desktop などから保管フォルダを検索・参照・書き込みできるようにする。
第 2 のバイナリ `oboegaki-mcp`（stdio、`rmcp`）で、索引は読み・ファイルにだけ
書く。**10-1 → 10-3 で「読める」を先に区切り**、書きは 10-4 以降。

- [x] **10-1. バイナリの足場と接続**（2026-09-11。rmcp 3.3、`make mcp`）
      `src-tauri/src/bin/mcp.rs` を `[[bin]]` に足し、`rmcp` で stdio の
      サーバを立てる。引数は保管フォルダ。`initialize` に答え、ツール 0 個で
      Claude Desktop から「接続できた」まで。`.mcp-ignore`（1 行 1 フォルダ）
      の読み込みと、`.trash` / `templates` / 管理フォルダの除外をここで持つ
- [x] **10-2. 読みのツール（検索・本文・一覧）**（2026-09-11。search_notes / read_note / list_notes / list_folders / list_tags。stdio の煙試験で確認）
      `search_notes`（`IndexDb::search`。`#タグ` / `after:` は本文と同じ書き方）
      / `read_note`（本文と更新時刻。長いものは先頭と「続きがある」印）/
      `list_notes`（フォルダ・タグで絞る）/ `list_folders` / `list_tags`。
      アプリが動いていない（`instance-oboegaki.lock` が無い）ときは問い合わせの
      前に `IndexDb::sync` を自分で走らせる。JSON 入出力のテストを純 Rust で
- [x] **10-3. 関連・履歴・resource**（2026-09-12。stdio の煙試験で確認）
      `related_notes`（`backlinks` + `related_signals` を `related::rank` で
      束ね、根拠ごと返す。**見せない場所は順位付けの前に落とす** — 後で
      落とすと limit がそのぶん減る）/ `note_history`（版の一覧。`at` に
      時刻を渡すとその版の本文。**場所ではなく時刻で名指す** — 版の場所を
      受けると vault の好きなファイルを覗ける）/ resource
      `oboegaki://note/<相対パス>`（percent 符号化。素の日本語で来ても読む）。
      **ここまでで「読める」を区切った**
- [x] **10-4. 書きのツール（作る・追記・今日）**（2026-09-12。stdio の煙試験で確認。実機の外部変更は 2026-09-18 に目視で確認）
      `create_note`（題名・本文・フォルダ・雛形）/ `append_to_note`（末尾。
      見出しを指定すればその節の末尾。節の切れ目は `section_end` — 規則は
      TS の `lib/section.ts` と同じで、コードの中の `#` は数えない）/
      `daily_note`（何度呼んでも同じノート）。書くのは `.md` だけ
      （`save_atomic`）。`Vault::create_in_with` / `template_text` を足して
      画面からの作成と同じ道を通す
      - [x] アプリが動いている状態で書いて、外部変更の流れが拾うことを実機で
            確認（未編集なら読み直し、編集中なら競合の 3 択）— 2026-09-18 に目視で確認
- [x] **10-5. 差し替えと片づけ（楽観ロック）**（2026-09-12。stdio の煙試験で確認）
      `replace_note` は `read_note` で得た更新時刻を必須にし、違えば断る
      （**一度書いたら時刻が変わるので、続けて書くには読み直しが要る**）。
      `move_note` / `trash_note`（ゴミ箱まで。空にはしない。ピン留めは断る =
      spec §7.3）。どちらも `history::rekey` で履歴を連れて行く（ADR-0042）。
      アプリが動いていないときは差し替えの前に `history::keep` で版を残す
      （動いていればアプリの保存が残すので二重に残さない）
- [x] **10-6. 同梱と設定の書き出し**（2026-09-12）
      **externalBin は要らなかった** — `[[bin]]` は Tauri の bundler が
      そのまま `Contents/MacOS/` へ入れる（計測ツールを features で外したのと
      同じ仕組み。実測 2026-09-12: `make app` した `.app` に
      `oboegaki-mcp` が入っている）。環境設定に「MCP」タブを置き（使う人の方が少ないので「一般」から分けた。
      要望 2026-09-12）「設定をコピー」で
      Claude Desktop 用の JSON 断片（本体の隣のバイナリのパス + 保管フォルダ）を
      クリップボードへ。manual_test に接続手順、hitofude-gap に「参照実装には
      無い」として記録、`.app` の大きさ（22MB = 本体 16.9 + MCP 6.3）を
      docs/bench.md に追記

## 第 11 群 — 共有フォルダで複数人が使う（[ADR-0052](adr/0052-shared-vault.md)）

保管フォルダを iCloud Drive・Google Drive・Dropbox・SMB に置き、各人が自分の
Mac で使う。「保管フォルダは共有してよい。索引とロックは各 Mac のもの」。
同時に同じノートを触ることは支援しない（競合の 3 択で受ける）。

- [ ] **11-1. 索引とロックの置き場を App Support へ**
      `app_data_dir/vaults/<保管フォルダのパスのハッシュ>/` に `index.sqlite` と
      `instance-oboegaki.lock`。置き場は `index_dir(root)` の 1 箇所で決め、
      `IndexDb::open(&vault.managed_dir())` の 25 箇所を置き換える。既存の
      `.OboeGaki/index.sqlite` と lock は開くときに一度だけ消す。CLAUDE.md T7 の
      文言も直す
- [ ] **11-2. 履歴の版に書き手（ホスト名）を混ぜる**
      版のファイル名に書き手を入れ、履歴の窓に添えて出す。古い版（書き手
      なし）は「不明」として読む。同じ瞬間に 2 人が残しても衝突しない
- [ ] **11-3. 競合コピーの名前に書き手を入れる**
      `名前 (競合 YYYY-MM-DD ホスト名).md`。同期ソフトの競合コピー
      （conflicted copy）はアプリから触らない
- [ ] **11-4. 共有フォルダでの実機確認**
      2 台の Mac（または 2 つのユーザー）で同じ保管フォルダを開き、別々の
      ノートの編集・同じノートの編集（3 択）・改名・フォルダ移動・ゴミ箱を
      manual_test に追加して通す。同期ソフトの触り直しで 3 択が出ないこと
      （3-26）も確認。Google Drive は**ミラーリング**で通し、
      ストリーミングで何が起きるか（索引作りでの全件取得・他人の保存が
      FSEvents で届くか）も見る（ADR-0052 の決定 4 は未実測の見立て）

## 第 12 群 — 提案から起こしたもの（2026-09-11。ADR-0053〜0060）

推し順は 12-1 → 12-2 → 12-3。いずれも既にある土台（索引の links 表・履歴・
検索結果）の払い戻しで、新しい依存がほぼ要らない。

- [x] **12-1. ノートの改名で `[[リンク]]` を書き換える**（2026-09-11）（[ADR-0053](adr/0053-rename-rewrites-wikilinks.md)）
      索引の `links` 表で旧題名を指すノートを引き、`[[旧|表示]]` も含めて
      書き換える（`wikilink::normalize` で判定、コードの中は除く）。件数を
      ステータスに。純 Rust でテスト
- [x] **12-2. 履歴の窓に差分を出す**（2026-09-11）（[ADR-0054](adr/0054-history-diff.md)）
      `lib/diff.ts`（行単位 LCS）。追加は緑・削除は赤・同じ行は畳む。比較の
      相手は「今の本文」か「1 つ前の版」。語単位の強調はしない（日本語で読めない）
- [x] **12-3. 保管フォルダ全体の検索と置換**（2026-09-11）（[ADR-0055](adr/0055-vault-wide-replace.md)）
      Rust に「対象を 1 件ずつ読んで書き換えて書く」共通の土台 `rewrite_notes`。
      検索結果の下に置換の欄。件数を先に見せ、正規表現は無し、コード・front
      matter の中は既定で除く。開いているノートは先に書き切る
- [x] **12-4. タグの改名・統合**（2026-09-11）（ADR-0055）
      タグ一覧の右クリック「名前を変更…」。既にある名前なら統合（確認 1 回）。
      判定は `tags.rs` のスキャナと同じ規則。12-3 の土台に乗せる
- [x] **12-5. やること一覧**（2026-09-11）（[ADR-0056](adr/0056-task-list.md)）
      索引に `tasks` 表（スキーマ世代 +1）。サイドバーに「やること」の節、
      未完了を期限が近い順に。押すと行へ飛ぶ、一覧の上で完了にできる
- [x] **12-6. どこからでも書き取り**（2026-09-11）（[ADR-0057](adr/0057-quick-capture.md)）
      `tauri-plugin-global-shortcut`（既定 Cmd+Shift+Space、変更可）。小さな
      窓に書いて Cmd+Enter で今日のノートの末尾へ。IME 手動チェック（T5）を通す
- [x] **12-7. `![[ノート名]]` の埋め込み**（2026-09-11）（[ADR-0058](adr/0058-transclusion.md)）
      行まるごとのときだけ読み専用 widget で描く（深さ 1 段）。`#見出し` で
      節だけ。HTML / PDF は展開、PowerPoint はコードのまま
- [x] **12-8. Word（.docx）書き出し**（2026-09-11）（[ADR-0059](adr/0059-docx-export.md)）
      `docx`（npm）で見出しスタイルに割り当てて組む。数式・Mermaid は PNG。
      設定タブは作らない。出来上がりの XML でテスト
- [ ] **12-9. 自動更新**（[ADR-0060](adr/0060-auto-update.md)。**署名・公証の後**）
      `tauri-plugin-updater` + GitHub Releases の `latest.json`。起動時に 1 回
      確かめて知らせる。勝手には入れない
- [x] **12-10. hitofude-gap.md の棚卸し**（2026-09-11 済み）（ADR 無し。文書の直し）
      「未実装」の行がほぼ全部古い（2026-09-11 に数えた: 79 行のうち 45 行が
      未実装の印で、コードで確かめると実装済みが 40 行以上。本当に開いている
      のは署名・公証と ULID id（パス鍵で代替と決めた）だけ）。行ごとにコードの
      場所（ADR・ファイル）を書いて直す。**差分表が嘘をつくと次の判断を誤る**

- [x] **12-11. 本文の文字色**（2026-09-11）（[ADR-0061](adr/0061-text-color.md)。NotePM の方式）
      `<span style="color: …">` を `color` / `background-color` の 2 属性・色名か
      16 進に限って受ける（判定は lib/text-color.ts に一本化）。エディタは
      タグを隠して中の文字に色（ダークでは明度を上げる）、ツールバーに
      「文字色」（6 色 + 消す）、HTML は組み直して通す、PowerPoint は run の色
- [x] **12-12. コードの配色を PowerPoint にも**（2026-09-11。ADR 無し）
      HTML の配色は既に 4-4 で実装済みだった（export-code.ts。棚卸しで見落と
      した）。PowerPoint は単色だったので、同じ字句の切り方で run に色を付ける
      （highlightCodeRuns。明るい地で読める組 CODE_RUN_COLORS）。解析は
      書き出しの前に App が済ませ、options.codeRuns で渡す

## 第 13 群 — 施錠付きノート（[ADR-0062](adr/0062-locked-notes.md)）

**まだ実装しない。** 決めることが 3 つ残っている（鍵の単位・守りたい相手・
鍵を忘れたときの扱い）。決まってから 13-1 に入る。順番も依存もそのときに
見直す — 下は「どれだけの仕事か」を見るための粗い割り。

- [ ] **13-0. 3 つを決める**（ADR-0062 の末尾）。決めた結果を ADR に書き足し、
      状態を「採用」に変えてから下へ進む
- [ ] **13-1. 暗号の芯（純 Rust）**
      XChaCha20-Poly1305 + Argon2id。`lock.rs` に封入・開封とファイル形式
      （頭に版・salt・nonce）。**鍵はメモリの外に出さない**。壊れたファイル・
      違うパスワード・版違いの扱いまでテストで固める
- [ ] **13-2. `.md.enc` を別の種類として通す**
      `vault::scan()` は拾わない（索引・検索・やること・リンク・MCP から
      自動的に外れる）。一覧には出すが本文は 🔒。**平文の道に混ぜない**
- [ ] **13-3. 施錠・解錠の窓と自動施錠**
      開くときにパスワードを聞く。開いている間だけ平文を持ち、一定時間で
      自動施錠。IME 手動チェック（T5）を通す
- [ ] **13-4. 履歴・退避からの除外（または暗号化）**
      版を残さないなら、その旨を窓に出す（ADR-0023 の「作り直せない」と
      同じ重さで伝える）。退避（クラッシュ復元）も平文で書かない
- [ ] **13-5. パスワードの変更と、既にあるノートの施錠・解錠**
      平文 → 施錠、施錠 → 平文の往復。**元のファイルを消す前に新しい方を
      書き切る**（途中で落ちても失わない）
- [ ] **13-6. 手引きと手動チェック**
      添付は隠れないこと・鍵を忘れたら読めないこと・メモリ上の平文は守れない
      ことを、同梱の手引きに**はっきり書く**。manual_test に一式

## 第 14 群 — Qiita へ投稿する（[ADR-0063](adr/0063-qiita-publish.md)）

**まだ実装しない。** 決めることが 3 つ残っている（タグの決め方・公開の既定・
画像の扱い）。決まってから 14-1 に入る。

- [ ] **14-0. 3 つを決める**（ADR-0063 の末尾）。API の仕様も公式ドキュメントで
      確かめ直してから、ADR の状態を「採用」に変える
- [ ] **14-1. トークンを Keychain に置く**
      環境設定「Qiita」タブで入れて、macOS の Keychain へ。**保管フォルダにも
      設定 JSON にも書かない**（ADR-0052: 保管フォルダは共有されうる）。
      入っているかだけを画面に出す（値は出さない）
- [ ] **14-2. 送る形に整える（純 Rust / 純 TS でテスト）**
      front matter と先頭の H1 を外し、H1 は title へ。タグを決め方に従って
      組む。**画像リンクを数えて返す**（警告に使う）。ここは通信なしで全部
      テストできる
- [ ] **14-3. 投稿と更新（Rust 側で叩く）**
      `POST /api/v2/items` と `PATCH /api/v2/items/:id`。TLS が要るので HTTP
      クライアントを足す。**トークンは WebView に渡さない**。失敗（401 /
      レート制限 / 通信断）をそれぞれ読める言葉にする
- [ ] **14-4. front matter に記事 ID を書き戻す**
      `qiita: <item_id>`。次からは同じ記事を更新する。**アプリから front
      matter を書く数少ない例**なので、ユーザーの明示操作の結果としてだけ書く
- [ ] **14-5. 投稿の小窓**
      タグの確認、限定共有／公開の別、**載らない画像の警告**を出してから送る。
      送ったあとは記事の URL を出す（開くのはブラウザ）
- [ ] **14-6. 向こうで編集されていたら止める**
      `updated_at` を覚えておき、食い違ったら断る（楽観ロック。MCP の
      `replace_note` と同じ構え）
- [ ] **14-7. 手引きと手動チェック**
      トークンの取り方・限定共有で出ること・画像は載らないことを、同梱の
      手引きに書く。manual_test に一式

## 第 15 群 — コードレビューの指摘（2026-09-13）

ADR 無し（作りの整理と、テストの穴埋め）。**順番はレビューで付けた優先順**
— 将来の事故を止めるもの → 実害が出るもの → すぐ終わるもの → 腰を据えるもの。

- [x] **15-1. 「捨てる」の規則を 1 か所に**（2026-09-13）
      ピン留めの削除ガード（spec §7.3）と、移動・ゴミ箱のあとの履歴の鍵の
      付け替え（ADR-0042）が、画面用（commands.rs）と MCP 用（mcp.rs）で
      **別々に書いてある**（エラー文言まで同じ文字列を 2 回）。書き手が
      3 人目（Qiita・共有フォルダ）に増えたとき、どれかが規則を落とす。
      vault.rs に 1 つ置いて両方から呼ぶ
- [x] **15-2. `.mcp-ignore` の判定を揃え、外部変更を拾う**（2026-09-13）
      判定が TS（mcp-hidden.ts）と Rust（IgnoreList::is_ignored）に別実装で、
      Rust だけが `.trash` / `templates` / ドット始まりも隠す。さらに画面は
      開いたときに 1 回読むだけで、監視は `.md` しか通さない（watcher.rs）
      ため、**手で直しても同期で降ってきても印が古いまま**。ADR-0052
      （共有フォルダ）と正面から噛む
- [x] **15-3. `make check` でテストコードも lint する**（2026-09-13）
      `cargo clippy` に `--all-targets` が無く、テストの中が素通り。付けると
      実のある指摘が 3 件出る（related.rs の定数 assert = 何も試していない、
      llm.rs の冗長なフィールド名、lib.rs の ASCII 判定）。日本語のテスト名で
      non_snake_case が大量に出るので `mod tests` に allow を付けてから
- [x] **15-4. 使われなくなった `pointerAt` を片づける**（2026-09-13）
      余白の修正で `activationHere` に置き換わったのに、export とテスト 4 件が
      残っている。**守っているつもりで何も守っていない**
- [x] **15-5. `[[…]]` の走査を減らす**（2026-09-13）
      TS のパーサ・Rust の `links_in`・Rust の `rewrite_line` が手書きで 3 実装。
      実際に食い違っていた（別名を書き換えだけが拾っていた = ADR-0064）。
      Rust の 2 つを 1 つにし、TS と同じ入力で突き合わせるテストを置く
- [x] **15-6. 細かい 3 つ**（2026-09-13）
      冒頭の抜き出しは**題名（H1）だけ落とす**に揃えた（`## 節` で始まる
      ノートで泡と一覧が食い違っていた）／一覧に「Claude に渡さない」の印
      （⊘）を出した／タグの飾りの上でも指差しになる（字の右端は
      `[start, end)` の規則どおり外）
- [x] **15-7. App.tsx から右クリックメニューを出す**（2026-09-13。ADR-0048 の続き）
      **判断があるもの 3 つ**（ノート・フォルダ・ゴミ箱）を
      `components/note-menu.tsx` の純関数にして、テスト 9 件で固定した
      （ピン留め中は捨てられない・直下では作るものだけ・渡す / 渡さないの
      切り替え・取り返しのつかない項目の danger）。3,592 → 3,506 行。
      残る 4 つ（新規・本文・アウトライン・タグ）は分岐がほぼ無い素の並びか、
      エディタの状態に密着しているので**そのまま**にした — 出すこと自体が
      目的ではない
- [x] **15-8. MCP バイナリの統合テスト**（2026-09-13）
      `src-tauri/tests/mcp_stdio.rs`。バイナリを起動して JSON-RPC を往復する
      （繋がって道具が 13 個並ぶ／資源として並び見せない場所は断る／読んで
      書いて楽観ロックが効く）。**手でしか確かめられないものは、いつか確かめ
      なくなる** — 10-2〜10-5 の煙試験をここに下ろした。0.55 秒で通る
      - [x] **15-8b. `commands.rs` の判断を下ろす**（続き。2026-09-17 におおむね済）
            履歴の鍵の付け替えは `Vault::carry_history` の 1 本に倒した（17-2）。
            退避の復元は `restore_pending`、フォルダの後追いは `rekey_moved_folder`、
            版の書き戻しは `restore_version`、Finder は `finder_target` に抜いて
            headless で試せる。残るのは索引の更新の配線だけで、そのままでよい
- [x] **15-9. MCP 側の「渡さない」の穴を 3 つ塞ぐ**（2026-09-14 のレビュー）
      15-2 は画面側だけだった。サーバは `.mcp-ignore` を**起動時に 1 回**しか
      読まず（Claude Desktop は常駐させるので、GUI で隠しても開き直すまで
      効かない）、`list_tags` は索引の集計を素通し（隠したノートにしか無い
      タグが存在ごと漏れる）、`daily_note` は `guarded` を通らない（今日の
      ノートを隠していても追記できる）。3 つとも回帰テストを先に置いて直し、
      `get_info` の instructions も実際の道具（差し替え・移動・ゴミ箱）に
      合わせた
      - [x] **15-9b. `replace_note` はアプリ起動中に版を残さない**（2026-09-14）
            「アプリが動いていればアプリの保存が残す」と前提していたが、
            watcher は `history::keep` を呼ばない。開いていないノートを Claude
            が差し替えると旧本文が消える（ADR-0023 / T7）。**MCP が必ず残す**
            ことにした — 直前の版と同じ中身なら `keep` が飛ばすので二重に
            ならない
      - [x] **15-9c. ツールの失敗が `isError` なしの本文で返る**（2026-09-14）
            `format!("error: …")` が成功応答として届き、モデルが本文と誤読
            しうる。13 の道具を `CallToolResult::error` に寄せ、`mcp_stdio.rs`
            は `isError` を見る
      - [x] **15-9d. `McpVault::open` がどんなフォルダにも足場を作る**（2026-09-14）
            設定 JSON の `args` を間違えると `~/Documents` などに `.OboeGaki`
            が生え、索引が全 `.md` を取り込む。`.OboeGaki`（旧 `.hitofude`
            でも可）が無い場所は断る。手引きの「困ったとき」にも足した
- [x] **15-10. 別名リンクの名前の正規化が TS と Rust でずれる**（2026-09-14）
      `activationAt` は `trim()` だけ、Rust `normalize` は空白の畳み込みと
      NFC。`[[会議  メモ]]` は索引ではつながるのにクリックで別ノートを新規
      作成していた。`wikilink-cases.test.ts` は自前の抽出関数で試していたので
      緑のまま — 抽出を `wikilinkTarget` に出して本番とテストの両方で使い、
      見本に二重空白・NFD・表示が空の 3 例を足した（Rust も同じ見本を見る）。
      `[[名前|]]`（表示が空）は別名扱いにせず、名前を残して `[[` `]]` だけ隠す
- [x] **15-11. メニューの ✓ がノートを切り替えるとずれる**（2026-09-14）
      `sourceMode` だけ `init` で引き継ぎ、フォーカス・タイプライタは新しい
      view で false に戻るのに `editorModes` は更新されなかった。`editorModes`
      を「開いた時点の値」を受ける関数にし、Editor が props で引き継ぐ。
      親フォルダ由来で隠れたノートの「Claude に渡す」は、押せるように見せず
      「「秘密」ごと隠れています」と理由を出す（`hiddenByAncestor`）。押されて
      も App 側で断る
- [x] **15-12. MCP の細かいもの**（2026-09-14 のレビュー。低優先。同日に直した）
      - [x] `history_text` の `mtime_ms` — `saved_at` はローカルの naive 時刻なので
            `Local.from_local_datetime` で読む（`and_utc()` は JST で +9h ずれた）
      - [x] `append_to_note` / `replace_note` / `daily_note` の read-modify-write
            を `McpVault` の Mutex で直列にした。同じノートへ 2 スレッドで 40 回
            ずつ追記して 1 行も消えないテスト
      - [x] ドット始まりの成分は**どの階層でも**隠す（`scan()` と揃える）。
            TS の `isHiddenFromMcp` / `hiddenByAncestor` も同じ規則に。`guarded`
            は `.md` 以外を「ノートではありません」と断る（`.mcp-ignore` や
            `foo.txt` を読ませない）
      - [x] `.mcp-ignore` の行と道を NFC に寄せて照合する
      - [x] `mcp_set_hidden` は `mcp::hidden_relative` を通す — 外の絶対パスは
            断り、綴りが違っても実体が同じなら中（canonicalize）。テスト付き
      - [x] `create_note` は front matter の**下**に見出しを置く
      - [x] `section_end`（Rust）と `sectionOf`（TS）のフェンスは、同じ字で同じ
            長さ以上の行でだけ閉じる（```` の中の ``` で閉じない）。両側にテスト
      - [x] `list_folders` の件数は見えるノートだけ数える
      - [x] `test_note_history_…` の末尾は `is_err()` だけを見る
      - [x] `pdf.rs` の OCR 依存テストは**そのまま**（決めた）。既存の
            `test_絵だけのPDFから文字を読む` と同じ前提で、CI が揺れたら
            そのときに両方まとめて見直す
- [x] **15-13. エディタの細かいもの**（2026-09-14 のレビュー。低優先。同日に直した）
      - [x] `activation.ts` の `onBlur` は `leave` と同じく位置も忘れる
      - [x] `peekExcerpt` はコードポイントで数えて切る（`Array.from`）。絵文字
            だけの本文で孤立サロゲートが残らないテスト
      - [x] `scrollDOM` の `scroll` で泡を隠す（ホイールで置き去りにしない）。
            聞き手は destroy で外れる
      - [x] 表のセルの中は `\|` と書く、を ADR-0064 に記した。`\|` の `\` は
            名前に入れない — TS `wikilinkTarget` と Rust `spans_in_line` を
            揃え、共有の見本に足した。素の `[[a|b]]` がセルで割れる現状は
            `wikilink-cases.test.ts` で固定
      - [x] `codeBlockAt` は ``` / ~~~ どちらでも 3 本以上なら受ける。
            `~~~ts:a.ts` でファイル名の行が帯の先頭になるテスト
      - [x] `NotePeek` の出す・待つ・隠す・遅れて届いた中身を捨てる（jsdom、
            5 件）と、`activationCursor` の聞き手が destroy で全部外れる（jsdom）
- [x] **15-14. UI の細かいもの**（2026-09-14 のレビュー。低優先。同日に直した）
      - [x] `relativeIn` は区切りで見る（`/vault2/a.md` は `/vault` の中ではない）
      - [x] `copyMcpConfig` は `ipc.mcpConfig` を通す（`invoke` の直叩きを 1 つ減らした）
      - [x] `.mcp-ignore` の読み直しと付け外しを `hooks/useMcpHidden` に出した
            （jsdom で 5 件。窓に戻ると読み直す・付け外すと一覧が差し替わる・
            読めなければ空）
      - [x] 印つきメニューの id は `fixtures/menu-checks.json` が両側の真実。
            TS は `lib/menu-checks.ts`（`setMenuChecks` の引数を `Record<MenuCheckId,
            boolean>` にして送り忘れを型で止める）、Rust は lib.rs の
            `toggle(...)` の字面を見本と突き合わせる
      - [x] `NoteRows.test` の自明な assert は「⊘ が題名と同じ button にある」に
      - [x] 読み込んだファイルの題名は `importTitle` で拡張子を落とす。一覧は
            `importFilter` と同じところから取るので、形式を増やせば両方に効く
- [x] **15-15. 文書・ビルドの細かいもの**（2026-09-14 のレビュー。低優先。同日に直した）
      - [x] `Makefile` のコメントは「MCP」タブに
      - [x] `docs/manual_test.md` の旧メニュー名（「Word に書き出し…」「読み込む…」
            「2 ページ」）を今の並び（エクスポート／インポート／4 つのタブ）に
      - [x] 「U-2」は参照実装の link_preview（リンク先を覗く泡）。hitofude-gap.md
            に行を足して定義した
      - [x] `scripts/make-samples.mjs` の出力先はファイルの場所から引く
      - [x] 2026-09-13 の実装を第 16 群として短く残した

## 第 16 群 — 実機の要望と修正（2026-09-13。記録漏れを 9/14 に起こした）

要望と実機報告から直したもの。詳細は各コミットの本文。

- [x] **16-1. 読み書きを試す見本**（529c57e）`make samples` で
      fixtures/samples/ に .md・画像・CSV・docx・pdf・pptx を作り直せる。
      説明はそこの README。テストは `samples.test.ts` と `pdf.rs`
- [x] **16-2. メニューの再編**（ab8906a・86feaf5・74fca9a・6dfcb69）
      書き出しと読み込みを「エクスポート」「インポート」に畳む／仲間ごとに
      並べ直す／「ウインドウ」と「サービス」を足す／状態を持つ 7 項目に ✓
      （`menu_checks`。決めるのは画面 = T2）／フルスクリーン／エディタ側の
      重複キー（`Cmd+/` など）を外す
- [x] **16-3. コードブロックのコピーの印**（bdeae7f・cb407b9・762486f・
      a1e4fb3・d877b55）印が出ない→帯の先頭の行末に置く／写したら知らせる／
      濃い地の中でも選択とキャレットが見える／印の大きさとファイル名ラベルの間
- [x] **16-4. `Cmd` の指差しと覗き見**（420c43d・c6c0a94・7fd0d0f。U-2）
      `Cmd` を押しながらリンクに重ねると指差しになり、400ms で冒頭の泡が出る。
      行の右の余白では押せるように見せない（`onText`）
- [x] **16-5. プレビューモード**（要望 2026-09-15。[ADR-0065](adr/0065-wysiwyg-mode.md)）
      カーソルを置いただけ・選んだだけでは記法を出さず、**書き込んでいる行**
      （打つ・消す・貼る）だけ §6.4 の規則で現す。直しは書式ツールバーから。
      ソースモードと排他。メニュー「編集モード」（インライン／ソース／プレビュー
      は排他で必ず 1 つに ✓、フォーカス／タイプライターは併用可）と歯車に ✓、
      `Cmd+Shift+P`。右上のボタン（NoteActions）は 3 つを巡り、今のモードを
      絵（鉛筆／`<>`／目）で見せる（`lib/edit-mode.ts`）。
- [x] **16-6. プレビューモードで表を直せる**（実機 2026-09-15）
      表・図・数式・囲みは書く面を持たないので、プレビューモードでもカーソルが
      入れば生に戻す（`touchesBlockZone`）。あわせて表の中の鍵を足した
      （`table-keys.ts`）: Enter で下に行、見出しなら区切りの下、空の行で抜ける／
      Tab で次のセル、見出しの端なら列を足し、最後の行の端なら行を足す／
      Shift+Tab で前のセル。見出しの行末に `|` を打つだけでは GFM の規則で
      表が壊れるので、列は鍵で足す
- [x] **16-7. 外部レビュー（2026-09-16）の照合**
      9 件のうち 7 件は古い状態を見ていた（App.tsx は 6,000 行ではなく 3,590 行で
      useState は 13、`stores/app.ts` は 56 行で invoke は `lib/ipc.ts` に既に
      分離、`escapeHtml` は `"` `'` も逃がしている、CSP は設定済み、Rust の
      テスト名の警告は `#[allow(non_snake_case)]` で 0、`eslint-plugin-react-hooks`
      は導入済みで `exhaustive-deps` は error、UI 部品のテストは 33 ファイル）。
      直したのは 2 件: `"oboegaki.side"` の直書きを `lib/side-pane.ts` に集めた／
      `llm.rs` の実ソケットの stub は、待ち受けできない環境で理由が読める形で
      落ちるようにした（黙って飛ばさない）
      判定は `touchesSelection` 1 か所に挟んだので、インライン・ブロック・表・絵に
      まとめて効く

## 第 17 群 — 棚卸しレビュー（2026-09-17。全体を見てテストの穴と改善点を洗った）

- [x] **17-1. Rust の正しさ 5 件**（2026-09-17）
      `Vault::folders()` が Finder 製の NFD の名前をそのまま返し、索引（NFC）と
      噛み合わず件数 0・中身空に見えた／一括置換・タグ改名・リンク書き換えが
      版を残していなかった（ADR-0055 の約束と食い違い。`link_rewrite::keep_version`）／
      本文の無い `- [ ]` が一覧に出るのに完了にできなかった（`set_task_done` が
      改行付きの行で印を見つけられない）／退避の復元に 1 件でも失敗すると
      `clear_all` で全部捨てていた（`restore_pending` に抜いて成功分だけ捨てる）／
      `purge_trash(0)` がゴミ箱を即刻空にした（最短 1 日）
- [x] **17-3. エディタ層の正しさ 6 件**（2026-09-17）
      中身の無い色 span（`<span style="color: red"></span>`）で mark が空になり
      CM6 が投げて hideMarkers ごと止まり、その文書の装飾が全部消えていた／
      プレビューモードで書式ツールバーを押すと記法が出ていた（コマンドの
      userEvent が素の "input" で前方一致に掛かる。`input.type` / `paste` /
      `drop` / `delete` だけを「書いた」と数える）／プレビューモードで行を移る
      たびに表・ブロックのウィジェットを全部数え直していた（`revealModeSwitched`
      に分けた）／脚注の定義行 `[^1]: 本文` の本文が隠れていた（LinkReference
      は生のまま）／閉じの無いフェンスで最後の行に帯が付かなかった／行頭で
      終わる選択（改行込みで行を選んだ形）が次の行まで箇条書きにしていた。
      日本語の字に続く URL を拾うのは**意図して**参照実装と違える（テストで固定）
- [x] **17-4. エディタ層の残り**（監査 2026-09-17。子項目を全部終えて 2026-09-18 に締めた）
      - [x] 引用の中の表・フェンスで 2 行目以降の `> ` が隠れない（Lezer は継続行の
            QuoteMark を葉ブロックの子に置く。`hideQuoteMarks` で潜る。2026-09-17）。
            引用の中の表を widget にするのは別（`tableDecorations` はトップレベルだけ）
      - [x] プレビューモードで水平線・埋め込みはカーソルで生に戻す（表・図と同じ
            `touchesBlockZone`）。行まるごとの画像は**絵のまま**と決めた（記法を
            出さないのが狙い。ADR-0065 に追記。2026-09-17）
      - [x] ベンチ（editor-bench.ts）の拡張スタックが Editor.tsx と乖離していた。
            `editor/extensions.ts` の `coreExtensions(opts)` に切り出し、本番とベンチが
            同じものを呼ぶ（Markdown の設定も 2 か所の複写を 1 つに。2026-09-17）。
            プレビューモードの計測は未（`docs/bench.md` の手順に加えるかは別途）
      - [x] 字下げした見出し（`  # 題`）の上げ下げ・循環（3 文字までの字下げを保つ）／
            `1. [ ] やる` がチェックボックスになる（番号は残す。2026-09-17）
      - [x] Setext 見出しの下線行は細い線として描く（`SetextRuleWidget`。空行を残さ
            ない）／表の中で選択があるときの Enter は既定に譲る／引用・フェンスの行
            クラスを可視範囲で切る（T6。2026-09-17）
- [x] **17-5. UI 層の正しさ 6 件**（2026-09-17）
      環境設定で履歴の「なし」（0）を選んでも設定が変わらなかった（数値欄の
      空対策 `value <= 0` が巻き込んでいた。`cleanSettingsPatch` で項目ごとに
      見る）／外部変更の読み直しに失敗すると未処理の reject で無反応だった／
      競合の問いがノートの切り替えを跨いで生き残り、別のノートに外の本文を
      入れられた（切り替えで捨てる・答えるときも今のノートか確かめる）／
      前のノートの遅い保存が今のノートの「開いた時の本文」を上書きし、同期ソフト
      の触り直しで偽の競合が出た／退避の復元に失敗すると件数が消えて選び直せな
      かった／`imageSource` が失敗まで永久に覚え、後から画像を置いても描かれなかった
- [x] **17-6. UI 層の残り**（監査 2026-09-17。子項目を全部終えて 2026-09-18 に締めた）
      - [x] テストの無い部品 6 つに足した（2026-09-17）: `GeneralPreferences`（7 件）・
            `AssistantPreferences`（6 件）・`CaptureRoot`（4 件）・`useCaptureShortcut`
            （6 件）・`lib/capture.ts`（2 件）・`stores/app.ts`（5 件）
      - [x] `ipc.ts` の包み 47 個をコマンド名と引数の表で固定（`fetchLists` の
            camelCase 組み替え、`replaceApply` の options の平坦化、`subscribeLlm` の
            3 本を含む。2026-09-17）
      - [x] App.tsx の `void` 呼びで catch の無い非同期ハンドラ 13 か所（書き出し・
            雛形・ゴミ箱・添付の掃除・同期）を `lib/run-command.runWithStatus` で
            包み、失敗を「○○できませんでした: …」とステータスに出す（2026-09-17）
      - [x] `useCaptureShortcut` と `CaptureRoot` が Tauri を直接呼んでいた（ADR-0049）。
            `lib/ipc` に `registerGlobalShortcut` / `unregisterGlobalShortcut` /
            `openCaptureWindow` / `closeCurrentWindow` を置いて通した（2026-09-17。
            ソースの中の動的 import は vitest で差し替えが 2 回目から効かず、試せなかった）
      - [x] `useCaptureShortcut` が `{ error }` を返し、App → PreferencesDialog →
            GeneralPreferences の欄の下に「登録できませんでした: …」と出す（2026-09-17）
      - [x] PowerPoint の表はセルごとの run で持つ（`\|` は字、`**` は装飾、空のセル
            は列を保つ）。Word: やることの印 ☐ / ☑、`![a|100]` の大きさ、コードの
            ファイル名、脚注の本文の番号、番号付きの開始値（2026-09-17）
      - [x] `stores/app.ts` の `refresh` の追い越し（世代番号で古い応答を捨てる。
            保管フォルダを変えたあとの前のフォルダの一覧も捨てる。2026-09-17）
      - [x] 重複を 1 つに（2026-09-17）: `lib/html-escape.escapeHtml`／App の
            `refreshOrStatus()`／`frontmatter.bodyText`／`hooks/useLatest`（App 7 +
            useNoteSync 7。lint が ref と知らないので依存配列に並べた 3 か所あり）
- [x] **17-7. 文書とビルドの陳腐化**（2026-09-17）
      `make ci` に eslint が無く CI と手順が違っていた／打鍵ベンチの手順のポートが
      2 か所とも違った（5173 / 5183 → 1430）／`.PHONY` に bump・icon・mcp が無い／
      CLAUDE.md のテストの手本が存在しないファイルを指し、dnd-kit の記述が残って
      いた／AGENTS.md が CLAUDE.md の古い分岐（47 行差）だったので「CLAUDE.md を
      読め」だけに／README が 0.1.0 時点の年表のままだったので今の姿に書き直した／
      manual_test.md の参照実装の残滓（auto_covered.md・QTest・Hitofude・
      `.hitofude/`・app-lite）と古いラベル 4 件／hitofude-gap.md のタブ数・索引の列・
      OboeGaki 側にしか無いもの／frontmatter.ts の ULID の説明（ADR-0042 と矛盾）
- [x] **17-8. TS と Rust の二重実装で共有見本が無いもの**（監査 2026-09-17。子項目を全部終えて 2026-09-18 に締めた）
      `fixtures/wikilink-cases.json` / `menu-checks.json` と同じ型で見本を置き、
      両側のテストがそれを読む。判明している食い違いは先に RED にする
      - [x] front matter の判定（`fixtures/front-matter-cases.json`。空の
            `---\n---\n` を TS も受けるようにした。2026-09-17）
      - [x] タスク印 `- [ ]` の判定（`fixtures/task-marker-cases.json`。Rust
            `task_marker` と `lib/tasks.taskMarkerOf` が読む。TS だけが `- [ ]a` を印
            と見なしていた。2026-09-17）
      - [x] 番号付きのやること（`1. [ ]` `12) [x]`）を索引でも受けることにした
            （GFM と同じ。`tasks::list_prefix_len` / `lib/tasks.taskMarkerOf`。見本に
            3 例足した。2026-09-17）。`input-assist` の Enter は番号を継続するだけで
            `[ ]` は付けない（そのまま）
      - [x] ファイル名の正規化（`fixtures/filename-cases.json`。Rust の書式文字の範囲を
            TS の `\p{Cf}` に寄せ、TS は「無題」と 200 バイトの切り詰めを足した。2026-09-17）
      - [x] `#タグ` の境界（`fixtures/tag-cases.json`。TS は `lib/tag-name.normalizeTag`
            を置いた。Rust は `#/` の空タグを索引に入れない。`>#a` のような行頭記号の
            直後は両側で違いうるが見本には入れていない。2026-09-17）
      - [x] 見出し節の切り出し（`fixtures/section-cases.json`。2026-09-17）
      - [x] `.mcp-ignore` の照合（`fixtures/mcp-ignore-cases.json`。TS も NFC に寄せた。
            2026-09-17）
      - [x] `fixtures/*.md` と `golden/*.json` はブロックの種類で突き合わせる
            （`editor/golden-blocks.test.ts`）。ranges は Qt の表現なので見ない。参照
            実装と**意図して違える** 5 行（GFM に合わない表・Setext 見出し）は
            DIVERGENT に書いた（2026-09-17）
      - [x] 低（2026-09-17）: `SearchQuery::filter_only` を消した／`.nvmrc`（24）を
            置き CI も `node-version-file` で読む／`package.json` の `preview` を消した。
            **`rust-toolchain.toml` は置かない** — 手元の rustc は Homebrew（rustup で
            ない）で、その場合この file は読まれず、CI の `stable` と手元は揃わない。
            Rust は edition/MSRV を Cargo.toml で見張るに留める
- [x] **17-2. Rust の残り**（監査 2026-09-17。子項目を全部終えて 2026-09-18 に締めた）
      - [x] 履歴の鍵の字面を 1 本に寄せた（2026-09-17）。`vault::history_key` が
            root もパスも実体に解決してから相対にし NFC に寄せる（無いファイルは親で
            解決）。`Vault::carry_history` が履歴を動かす唯一の道になり、trash /
            restore / rename / move_note / フォルダの改名・移動（`rekey_moved_folder`）
            が全部そこを通る。commands の `rekey` 3 か所と MCP・link_rewrite の
            `format!("path:…")` を倒した。restore も版を連れて戻る（trash と対称）
      - [x] `Vault::rename` は拡張子を保つ（`.markdown` を `.md` にしない。2026-09-17）
      - [x] 雛形の読みを `read_note` に（Shift_JIS / CRLF の雛形。4 か所。2026-09-17）
      - [x] `#/` `#//` の空のタグを索引に入れない（17-8 の見本で固定。2026-09-17）
      - [x] MCP の stdio 往復を 13 道具すべてに（`list_notes` の folder + tag・
            `list_folders`・`list_tags`・`related_notes` の limit・`note_history` の一覧と
            at・`daily_note`・`move_note`・`trash_note`・符号化していない日本語 URI の
            `resources/read`。2026-09-17）
      - [x] `history_restore` を `restore_version` に抜いて headless で（今の内容を残して
            から書き戻す・別のノートの版は断る）、`import_read` の 256MB 上限をスパース
            ファイルで（2026-09-17）
      - [x] `link_rewrite` の `failed` 枝（読めないノートは数えて残りは書き換える）、
            `history::rekey` の時刻でない名前が両側にあるときの枝番の退避（2026-09-17）
      - [x] 意味の薄いテスト: Finder は `finder_target` に判断を抜いて実物のフォルダ・
            ファイル・外で見る／`{{date:}}` の空書式は印を残す（2026-09-17）。`ocr` の
            「10 秒未満」は**そのまま**（実行環境で揺れる計測なので閾値を締めない。
            ADR-0027 の実測は 0.85 秒）

## 第 18 群 — 要望（2026-09-17〜）

- [x] **18-1. 番号付きリストを字下げしたら番号を振り直す**（要望 2026-09-17、
      ADR-0066）。Tab / Shift+Tab でそのリスト（空行で切れるまで）を上から振り直す。
      入れ子は 1 から、親は続きから、先頭の番号は保つ。Enter の継続は §5.5-3 の
      まま振り直さない（`input-assist.renumberList`。2026-09-17）

## 第 19 群 — リファクタリング（2026-09-18。全体レビューで決めた 5 段。挙動は変えない）

レビューの所見: T1〜T7 は守られている。破れは `vault → mcp` の依存 1 本と、App.tsx
からの Tauri 直呼び 17 か所。問題は「同じ形の繰り返し」と巨大ファイル
（App.tsx 3,603 / live-preview.ts 2,214 / vault.rs 3,453 / commands.rs 1,993）。
判断 4 点（2026-09-18）: 行内数式は現状維持（行内の記法として扱う。ADR-0065 の語を
「数式ブロック」に直す）／Hr・Setext・FileName の `ignoreEvent` を他と揃えて
`false`／`src/markdown/` を新設して純粋な構文の知識を集める（Lezer 拡張は editor に
残す。ADR-0067）／`NoteService` は 2 段階（食い違いの芽を先に潰し、共有層は最後に
読み系から）。各段は refactor コミット（版は上げない）で `make check` を通す。

- [x] **19-1. 安全網**（2026-09-18）: エディタのテスト 24 ファイルを本番と同じ構文木
      （`markdownConfig()`）に統一し、`src/editor/test-utils.ts`（`LANG` / `stateOf` /
      `press`）に足場を寄せた（`stateOf` 7 種・`press` 2 コピーを吸収）。Rust は
      `#[cfg(test)] mod test_support`（`temp_vault` / `note` / `at`）で 3 点セット 87 か所
      と重複ヘルパ 4 つを置き換えた。jsdom の `afterEach(cleanup)` 36 ファイルは
      `src/test-setup.ts`（vitest の setupFiles。document が無ければ何もしない）に
- [x] **19-2. 純粋な移動**（2026-09-18）: `vault.rs`（3,453 行）→ `vault/`（mod / paths /
      text / scan / notes / templates / attachments / daily / seed / trash / history_carry。
      最大 676 行。外から見える名前は mod.rs で再エクスポート）。`lib.rs` のメニュー構築と
      「について」→ `menu.rs`（lib.rs は 217 行に。`include_str!` の自己参照テストも追従）。
      `live-preview.ts`（2,214 行）→ `live-preview-{reveal,widgets,table-data,zones,theme}.ts`
      （階層は増やさず接頭辞で並べる。本体は 519 行、`export *` で import 先は変えない）。
      `ensure_layout` の `mcp::ensure_ignore_file` 呼びを `commands::vault_open` と
      `McpVault::open` へ移し、vault → mcp の依存（層の逆転）を解いた
- [x] **19-3. 一本化の小物**（2026-09-18）
      - [x] Rust（2026-09-18）: `CmdError` / `CmdResult`（`From` で `?` に。commands.rs の
            `map_err(|e| e.to_string())` 66 か所が 1 つに。mcp.rs の 23 か所は
            `Result<_, String>` の API を rmcp が読むので据え置き）／`front_matter::split`
            `body`（7 か所）／`vault::nfc_string`（10 か所）`is_skipped`（3）`split_name`（4）
            `ensure_trailing_newline`（2）／`history::Version::stamp`（GUI と MCP で別々に
            決めていた版の時刻の字面）／`history_read` `restore_version` を `read_note`
            に（Shift_JIS の版が読めなかった）／`IndexDb::related_notes`（関連ノートの
            2 実装を 1 本に）／index_db の `note_meta` `like_escape` `purge_rows`
            ／`vault::is_markdown` を history にも／テスト専用の `pub` 5 つを下げた。
            **やらなかった**: `guarded` 2 種の統合（GUI は絶対パスで未存在も許す、MCP は
            相対で `.md` 限定 — 意図が違う）、`llm_generate` の引数 10 個の構造体化
            （TS の ipc 側の引数の形も変わる。19-4 の `lib/ipc` 拡充と一緒に）
      - [x] `src/markdown/`（ADR-0067。2026-09-18）: front-matter（範囲・本文・`key: 値`）・
            image-size・math-span・fence-info・tasks・tag-name・section・syntax（リスト／
            やること／見出しの正規表現 9 定義を 1 か所に。意図して違う 3 つは名前で区別）。
            eslint の `no-restricted-imports` で CM6・React・Tauri・他層を読まないことを
            守る。`lib → editor` は 15 本 → 6 本（残りは Lezer 拡張 2 本・`Text` 型を受ける
            囲み 2 本・`renderMath`・言語解決。意図して残す。ADR に理由）
      - [x] TS の小物（2026-09-18）: `lib/day.stampValue` `clockValue`（日付整形 4 か所）／
            `markdown/prose-lines.proseLines`（フェンス走査 3 か所。front matter の判定も
            front-matter.ts に揃えた）／`slide-split.buildDeck`（デッキ構築 3 か所）／
            `components/Dialog`（殻 9 コピー。Esc と focus trap は足していない — 足すなら
            ここ 1 か所）／`components/SideSection`（節ヘッダ 3 コピー。フォルダは別）／
            `lib/context-menu.menuAt`（右クリック 4 か所）／`--accent`（`#0a84ff` 11 か所）
            と死にセレクタ 2 つ／自ファイルでしか使わない `export` 17 個を外した
      - [x] `SimpleWidget`（描くだけの widget 5 つの基底。2026-09-18）。Hr / Setext /
            FileName は `ignoreEvent` を書き忘れて既定（渡さない）になっていて、水平線を
            マウスで押しても生に戻らなかった → 揃えて直した（fix）。判断 1（行内数式は
            行内の記法のまま）は ADR-0065 の語を「数式ブロック」に直して記録: Rust `CmdError` + `From`（`map_err(|e| e.to_string())` 88 か所）
      ／NFC・封じ込め・front matter 剥がし・skip-dir・stem 分割の横断ヘルパ
      ／版の時刻整形と `read_note` の統一、関連ノートの計算を `related.rs` に
      （NoteService の前半）／`src/markdown/`（image-size・math の走査・fence info・
      frontmatter の解析部・正規表現集約・tasks/tag-name/section）／日付整形 4 か所・
      フェンス走査 3 か所・デッキ構築 3 か所／`Dialog` の殻 8 コピー・`SideSection`
      4 コピー・`useContextMenu` 8 か所／`SimpleWidget` と `ignoreEvent` の揃え
      ／`#0a84ff` 11 か所を `--accent` に、死にセレクタ 2 つ／未使用 export 約 40
- [x] **19-4. App.tsx の分解**（2026-09-18。3,603 → 2,909 行。残りは下に）
      - [x] `lib/ipc` に invoke 10 本・OS の窓 8 本・購読 4 本を足し、App と部品の Tauri
            直呼び 20 か所を寄せた。`src/` で `@tauri-apps/*` を読むのは lib/ipc.ts だけ
      - [x] `hooks/useExport`（書き出し・印刷・取り込み。約 320 行、テスト 5 本）／
            `usePreferences`（設定・文字サイズ・PowerPoint。置き場を引数に。テスト 3 本）／
            `useOutline`（目次と統計。テスト 3 本）。フォントの候補の測りは
            PreferencesDialog の中へ
      - [x] 右クリック 5 種（本文・歯車・新規・タグ・目次）を `editor-menu` / `gear-menu` /
            `side-menus` の純関数に（note-menu と同じ構え。テスト 8 本）
      - [x] `run-command.failureText` で失敗の文を 1 つの形に（「〜に失敗:」5 か所を揃えた）
      - [x] PptxPreferences を `PptxSections`（7 節）と `patch(group, part)`（18 か所）に
            （643 → 295 行）
      - [x] `hooks/useAppMenu`（2026-09-18）: 印は全部まとめて送り（同じ中身なら送り直さ
            ない）、押されたら最新の動作を呼ぶ。55 項目の表は `actions` として渡す形に。
            `checks` は MenuChecks 型なので送り忘れは型で気付く（テスト 2 本）
      - [x] `hooks/useNoteCommands`（2026-09-18）: 開いているノート（本文・カーソル・
            世代・見出しの控え）と、開く／閉じる／作る（無題・雛形・日次・同梱）／改名
            （題名の欄・保存後の見出しの追従）／ゴミ箱（1 件・まとめて）／ピン／戻す／
            完全削除／空にする／移す。同期は口（NoteSyncPort）で受け、Tauri は lib/ipc。
            App.tsx は 2,909 → 2,625 行（テスト 6 本）。flush → refresh → openNote の
            並びはこの hook の中に集まったので、1 本化の必要は消えた
      - [x] `export-docx` の golden（2026-09-18）: ブロックの種類を全部含む 1 本の Markdown
            から組んだ document.xml / numbering.xml を `fixtures/golden/export-docx.*.xml`
            に固定（ハイパーリンクの r:id は docx が乱数で振るので揃えてから比べる）。
            意図して変えるときは `npx vitest run -u` で写しを更新して差分を見る
      - [x] `buildDocx` の分割（2026-09-18）: `lib/docx-blocks.DocxEmitter`（トークン →
            Word の段落・表・画像。runsOf / paragraphFor / codeBlock / closeParagraph）と、
            Document の組み立てだけの export-docx（593 → 115 行）。golden の字面は 1 字も
            変わっていない
      - [x] Rust `llm_generate` の引数 10 個を `GenerateRequest` に（2026-09-18。ipc は
            `{ request }` で送る。`too_many_arguments` の allow が消えた）
      - 据え置き: App.tsx に残る `setStatus(String(error))`（Rust の文をそのまま出す
            意図。hook へ移した分を除いて 8 か所 — 2026-09-18 に数え直し）
- [x] **19-5. 大きい投資**（2026-09-18）
      - [x] `note_service.rs`（2026-09-18）: 一覧（フォルダ × タグ）・フォルダと件数・
            タグと件数・版の一覧と時刻での引き当て。GUI（commands）と MCP が別々に組んで
            いた読み系を 1 本にし、`keep`（見せないノートを数えない）だけを MCP が被せる。
            `IndexDb::folder_counts` は消えた。**書き系（ゴミ箱・移動・日次）は既に
            Vault の 1 本を両側が通っている**ので層を足さない（差は Suppressor と索引の
            後追い = GUI だけの仕事）
      - [x] Run 中間モデル（ADR-0068。2026-09-18）: `markdown/runs.ts`（`Run` / `plainText`
            / `sameStyle`）を Word とスライドで共有。markdown-it 側は `lib/export-runs.
            inlinePieces`（run / break / image の断片）。export-docx の style stack は
            消えた。解析器は 2 つのまま（理由は ADR）: `NoteService` の読み系 5 操作（一覧・読み・検索・版・関連）
      → 書き系。書き出しの `Run` 中間モデル統合（docx と slides の `runsOf`。別 ADR）

## 第 20 群 — リファクタリングの続き（2026-09-18。第 19 群の後に挙げた提案を順に）

順番は「安く済んで守りが固まるもの → App.tsx のもう一段 → 性能の再計測 →
Rust / ipc の大きいファイル」。挙動を変えるのは Dialog の Esc と focus だけ（fix）。

- [x] **20-1. 守りの固め**（2026-09-18）
      - [x] `clippy::unwrap_used` を Cargo.toml の `[lints]` に固定（本番コードの unwrap は
            0 だった。テストは lib.rs の `cfg_attr(test, allow)`、結合テストは自分で allow）
      - [x] `Dialog` に Esc・focus の面倒を 1 か所で（開いたら中へ、autoFocus が居れば
            奪わない、Tab は中で回す、閉じたら開く前の場所へ — 呼び手が別へ移して
            いたら尊重。変換中の Esc は IME の取り消しなので閉じない = T5）。
            PromptDialog / FuzzyPalette / ListPalette が各自で見ていた Esc を外した
            （二重に閉じない）。テスト 11 本
      - [x] TASKS の据え置きの数を直した（12 → 8）
- [x] **20-2. App.tsx のもう一段**（2026-09-18。2,623 → 2,404 行）
      - [x] 右クリック 8 種を `OpenMenu`（判別共用体）1 本と `components/AppContextMenus`
            に。同時に 2 つ開くことが型で表現できない。項目は各 *-menu の純関数のまま、
            押したときの動作（actions）は App に残す。テスト 15 本
      - [x] ダイアログ 14 種（16 本の useState）を `OpenDialog` 1 本に。決定の処理は
            対象を引数で受ける（状態を読み戻さない）。名前を 1 つ聞く窓 5 種は
            `components/app-dialogs.promptSpec` で字面を決めて PromptDialog を 1 回だけ描く。
            テスト 6 本
      - **やらなかった**: 左ペイン（`<aside className="note-list">` 約 200 行）の部品化。
            数えると props が 45 本になり、App から見て「何を渡しているか」が今の JSX より
            読みにくくなる。切るなら先に一覧の状態（検索・絞り込み・並び・選択・ドラッグ）
            を hook に寄せてからで、それは別の回
- [x] **20-3. 性能の再計測**（2026-09-18。docs/bench.md に記録）
      - 検索: 最悪 2ms（合格。5,000 ノート）。索引の初回構築 2.4 秒・再同期 34ms
      - 打鍵: 内蔵ブラウザ（Chromium、非表示タブ）で**タブを作り直して 1 回だけ測る**と
            p50 1.7ms / p95 3.3〜3.5ms（4 回とも。**合格**）。同じタブで開き直すと
            16〜27ms に膨らむが、それは環境の癖でリファクタ前（fcd11b1）も同じ。
            採り方の注意は bench.md 注 2
      - 起動: **353〜372ms**（合格。2026-09-18 に測り直し。組み直した直後の 1 回目は
            674ms、初回起動は 1,921ms — dyld / Gatekeeper の読み込み分で、2 回目以降は
            350ms 台）。起動中のおぼえがきがあると二重起動防止で測れないので、終了してから
- [x] **20-4. Rust の大きいファイル**（2026-09-18。純粋な移動、外から見える名前は変えない）
      - [x] commands.rs（2,008 行・77 コマンド）→ `commands/`（app / notes / history / folders /
            trash / recovery / assets / llm / system / text の 10 本、最大 396 行）。mod.rs は
            全部の子が使う土台（CmdError・WatchState・guarded・decode・index_one）だけ。
            テストは試している関数の側へ
      - [x] mcp.rs（1,528 行）→ `mcp/`。rmcp のマクロは bin/mcp.rs にしか無く、lib 側は
            純 Rust だった。McpVault（本体）は mod.rs に残し、無視リスト（ignore 287 行）・
            URI と設定断片（uri 119 行）・節の終わり（section 151 行）を横に出した。
            mod.rs は 1,015 行（うちテスト 590 行）
- [x] **20-5. `lib/ipc.ts`**（2026-09-18。872 行・export 114 → `ipc-{notes,history,assets,
      llm,text,app,os}.ts` の 7 本、最大 335 行。`ipc.ts` は `export *` の顔で import 先は
      変えない）。「`@tauri-apps/*` を読むのは ipc だけ」はこれまで規約だけだったので、
      eslint の `no-restricted-imports` を `src/**`（`ipc-*.ts` とテストを除く）に足して
      見張る形にした

- [x] **20-6. 依存の更新**（2026-09-18）: マイナー一式（CodeMirror・React 19.3・jsdom・
      markdown-it・prettier・typescript-eslint など）を 1 コミット、メジャーは 1 つずつ —
      vite 8 / vitest 5 / plugin-react 6（同じ組。vite build は rolldown に）、mermaid 12
      （内蔵ブラウザで flowchart と sequenceDiagram を描いて確認）。
      **TypeScript 7 は見送り**: typescript-eslint 8.70 が「TS 7.0 は未対応」で止まる。
      typescript-eslint が追従したら再挑戦（5.8.3 のまま）
- [x] **20-7. ハイライトの書き出し**（2026-09-18）: 提案では「`==印==` が書き出しで効か
      ない」と書いたが、この方言のハイライトは `::印::`（spec §3.4）で、HTML / Word には
      既に出ていた。見本が方言と違っていただけなので、Word の golden の見本を `::印::`
      に直して `<w:highlight>` が出ることを golden が見るようにした

## 第 21 群 — 全体レビューの指摘（2026-09-24。Rust / エディタ / lib / UI の 4 観点）

c109393（0.5.63）を対象に 4 観点で読んだ。IPC の契約（77 コマンド・8 イベント）は
両側で一致、HTML 書き出しの逃がしは堅く、T1〜T7 は守られている。直すのは下の順。

- [x] **21-1. 版を残せなければ書かない、を 1 本に**（2026-09-24。fix）
      - [x] `history::keep` の同秒上書き: 中身の違う版が同じ秒に来たら**秒を進める**
            （rekey と同じ）。MCP の差し替えを 1 秒以内に 2 回呼ぶと旧本文が消えていた
      - [x] `Vault::write_with_version(path, before, after)` を置き、「読んで書き戻す」
            8 か所を全部通す: やることの完了・ピン・日次の追記・改名の見出し・MCP の
            追記と差し替え・一括書き換え（置換・タグ改名・リンク書き換え）・版の復元。
            うち 5 か所はこれまで版を残していなかった。テスト 6 本（`lock_history`）
- [x] **21-2. 高の残り**（2026-09-24。fix）
      - [x] Word 書き出しの画像: `lib/image-bytes`（data URL の種類と大きさ。PNG / JPEG /
            GIF / BMP のヘッダを読む）を置き、Word が受ける 4 種はそのまま、SVG と WebP
            は `rasterizeForDocx` で PNG にしてから渡す。以前は PNG しか受けず JPEG が
            黙って消えていた。テスト 6 本
      - [x] 環境設定の「タブ幅・行番号・字下げコード」を本文の Editor にも渡した（7-4 の
            2026-09-06 から参照ペインにだけ渡していた）。`indentedCode` はパーサ構成なので
            key に含める。App.tsx は丸ごとマウントすると IPC の差し替えが重いので、
            `App.wiring.test.ts` で字面を見張る（Rust の include_str! 自己参照と同じ構え）
      - [x] 表の Tab / Shift+Tab: セルの帰属を中身の終端ではなく縦棒の位置（`bound`）で
            決める。セル末尾の空白に居ると隣のセルと誤判定して列を足していた
      - [x] front matter だけで末尾改行の無い文書: 閉じ `---` の直後への入力は改行を
            補ってから本文へ（以前は `---あ` になり YAML が丸ごと本文化した）
- [ ] **21-3. 中（同じ型のものをまとめて）**
      - [x] Rust（2026-09-24。fix）: `move_folder` / `create_folder` は行き先の実在する
            親を実体に解いて封じ込め（`inside_or_unborn`）／索引の `upsert` / `remove` は
            `relative_key` で実体パスも相対に解き、解けなければ Err（以前は無音で素通り）
            ／`.mcp-ignore` は `save_atomic`／`task_complete` は一覧の文も受けて
            `tasks::complete_matching` で突き合わせ、ずれていれば断る（ipc・TaskSection・
            App も text を渡す）／`vault_open` は Busy を 40ms × 3 回まで試し直す（MCP の
            `app_running` がロックを取って離す一瞬と重なる）。テスト 4 本
      - [x] エディタ（2026-09-24。fix）: `blockWidgetField` はキャレット外でブロックの中が
            書き換わったゾーン（`touchesRange`）も差し替える／`tableField` は表の中だけの
            編集（行の増減なし）なら触った表だけ `refreshTableZones` で差し替え、カーソル
            移動もリビールが変わった表だけ（以前は全表を作り直していた）／
            `CheckboxWidget` の等価は印の状態だけにし、押されたときに DOM から位置を
            求める／引用・リストの中の ```mermaid はコードの帯で見せる。テスト 6 本
      - [x] lib（2026-09-24。fix）: PowerPoint の表は列数を見出しの行に揃える（GFM。
            pptxgenjs は 1 行目で gridCol を数える）／見出しの色 span を題から落とす
            （`plain` が HTMLTag を捨てる）／`imageSource` の壊れた `%` は null／行内脚注
            `^[…]` の本文側の番号を脚注側と同じ id+1 に／`graph.ts` の点・辺・Mermaid の id
            を同じ `titleKey` で引く。テスト 5 本。
            **据え置き**: `pptx.ts` の数値リテラル（フッタの高さ・扉の字の大きさ・カードの
            余白など十数か所）。用紙で変わる幅・高さは `slideMetrics()` から取っていて、
            残っているのは字の大きさと余白の定数。SlideMetrics に寄せるのは設計の判断
            （用紙に比例させるか固定か）が要るので別の回に
      - [x] UI（2026-09-24。fix）: 保管フォルダを替えたら検索・絞り込み（useSearch）と
            選択（App）を捨てる／`useAppMenu` に `allow` を足し、窓が開いている間は
            メニューバーから保存と「クイックオープンを閉じる Cmd+O」しか通さない／環境設定
            のキャンセルは PowerPoint の設定も戻し、Esc と背景もキャンセル（OK は右下の
            ボタンだけ）／書き取り窓は送っている間 textarea を止めて二重に送らない／
            `showLinkGraph`・手引きを置く・印刷・`[[新規]]` からの作成を `runWithStatus`
            で包む／`openNote` に世代ガード（後から開いた方が勝つ）。テスト 6 本
- [ ] **21-4. 低（控え）**: 引用内リストの Enter で `- ` が落ちる／タブ字下げのリストで
      補助が崩れる／右クリックメニューが Esc で閉じない／改名で EditorView が作り直され
      Undo が消える（`[initialDoc]` 依存）／アシスタントの答えがノートを替えても残る／
      `::**b**::` の中を書き出しが解析しない／CSS に無いクラス 4 つ（history-diff・
      saved-search-name・task-note・task-due）／`RelatedNote` `HistoryEntry` の GUI と MCP
      二重定義／同じ小関数の二重化 5 組（leadWidthCh≡indentWidth など）／テストにしか
      使われていない pub 4 つ／`autosave` が権限・xattr を落とす（未実測）／`.tmp` の
      掃除が無い／`resetPreferences` が 4 項目を戻さない／HistoryDialog の「60 分」決め
      打ち／`note_write` の force 常に false とコメントの食い違い／コメントと実装のずれ
      数か所（`vault-changed` の kind・menu-checks の数・ipc の説明の付け違い）

## 待ち — 外部要因でブロック中

- [ ] **署名・公証**（TASKS 0-C）Apple Developer アカウント待ち
- [ ] **配布バリアント（Lite 版）**（R-1〜R-3）4-2 Mermaid 導入で DMG が
      太ったら検討。現状 5.4MB なので不要

---

## 完了（移行フェーズ 2026-08〜09）

移行そのものの記録。詳細は git log と各 ADR（0034 以降）。

- [x] 技術検証スパイク 3 本（flanking オラクル / IME / Tauri 足場）全 GO
- [x] コア: Lezer 拡張（強調緩和・打消し・ハイライト・タグ・WikiLink）を
      参照実装 inline_scanner とオラクル等価に
- [x] ライブプレビュー（リビール・チェックボックス・画像・表 = HTML widget）
- [x] 入力補助（Enter 継続 / 2 段階解除 / Tab、IME ガード）と書式コマンド
- [x] vault / 索引（FTS5 trigram + LIKE / tags / スキーマ世代）/ watcher /
      履歴（間引き・rekey・prune）/ ゴミ箱（移動と戻す）
- [x] UI: ノート一覧（並び順・独立スクロール）・タグ一覧・アウトライン
      （Cmd+5）・クイックオープン・メニューバー・検索・履歴・競合 3 択
- [x] HTML 書き出し（markdown-it + 画像埋め込み）
- [x] 集中モード / タイプライタモード / ソースモード（Cmd+/）
- [x] 性能 3 基準の合格（docs/bench.md: 検索 2ms / 打鍵 p95 13.9ms / 起動 903ms）
- [x] 改名 ↔ H1 同期（ADR-0005）・索引同期の背景化・`make dmg`
- [x] App.tsx の分割（2026-09-07、ADR-0048）: 窓・パレット・ツールバー・
      ステータスバー・アシスタント・サイドバー・右クリックメニューの描画を
      `src/components/` へ（23 段・6,020 行 → 3,443 行）。部品は jsdom +
      Testing Library で単体テストできる形に。次は状態と処理の hooks 化
