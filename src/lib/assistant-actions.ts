/// アシスタントのボタン（要望 2026-09-04: アイコン + Tips）。
///
/// **絵は線で描く**（書式ツールバーと同じ判断）。currentColor が効くので
/// テーマから浮かない。16×16 で、太さも書式のアイコンに揃える。
///
/// アイコンだけなので、**呼び名と説明は Tips（title）と読み上げが担う**。

export type AssistantAction = {
  id: "summary" | "review" | "related" | "stop";
  /// 読み上げに出す短い名前
  label: string;
  /// Tips に出す説明。**呼び名を含める**（アイコンから名前が分からない）
  hint: string;
  paths: readonly string[];
};

export const ASSISTANT_ACTIONS: readonly AssistantAction[] = [
  {
    id: "summary",
    label: "要約",
    // **できることだけを書く。** 「3 行にまとめる」と書いていたが、
    // モデルが従うとは限らない（実機報告 2026-09-04）。守れない約束を
    // Tips に書かない
    hint: "要約",
    // 行が短くなっていく = まとめる
    paths: ["M2.5 4h11M2.5 8h11M2.5 12h5.5"],
  },
  {
    id: "review",
    label: "レビュー",
    hint: "レビュー（直したほうがよいところを挙げる。書き換えはしない）",
    // 吹き出し = 指摘。**直した文は書かない**という約束の絵
    paths: [
      "M2.5 3h11a1 1 0 0 1 1 1v6a1 1 0 0 1-1 1H7.5l-3.5 2.5V11h-1.5a1 1 0 0 1-1-1V4a1 1 0 0 1 1-1z",
      "M4.5 5.8h7M4.5 8.4h4.5",
    ],
  },
  {
    id: "related",
    label: "関連",
    hint: "関連するノート（索引を引くだけ。Ollama が無くても出る）",
    // 結ばれた点 = 関係。リンクの図と同じ言い回し
    paths: [
      "M4.4 6.2 10 4M4.6 9.4l5.2 2.2",
      "M3.2 6.5a1.6 1.6 0 1 0 0 3.2 1.6 1.6 0 0 0 0-3.2z",
      "M12.2 2.2a1.6 1.6 0 1 0 0 3.2 1.6 1.6 0 0 0 0-3.2z",
      "M12.2 10.6a1.6 1.6 0 1 0 0 3.2 1.6 1.6 0 0 0 0-3.2z",
    ],
  },
  {
    id: "stop",
    label: "止める",
    hint: "止める（そこまでの答えは残る）",
    paths: ["M4.6 4.6h6.8v6.8H4.6z"],
  },
];

/// 質問（テキスト欄の隣）。並びの外なので別に持つ。
export const ASK_ACTION = {
  label: "質問",
  hint: "質問（索引で材料を探して読ませる。読んだノートも出す）",
  // 送る = 紙飛行機
  paths: ["M14 2 2 7l4.6 1.8L14 2z", "M14 2 6.6 8.8 7.4 13.4 9.8 10.2"],
} as const;
